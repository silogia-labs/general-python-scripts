---
phase: 03-scoring-metadata-packaging
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - quizify-csv-to-json-webhook/README.md
  - quizify-csv-to-json-webhook/tests/test_readme_help_alignment.py
autonomous: true
requirements: [OPS-01]
tags: [docs, readme, python, cli]

must_haves:
  truths:
    - "quizify-csv-to-json-webhook/README.md exists with all 10 sections in D-11 order"
    - "README Configuration table documents quiz_title precedence (CLI > env > future CSV column > \"\") in markdown"
    - "README Quickstart shows: python quizify_csv_ingest.py docs/quizify-submissions.csv --quiz-title \"Autoevaluacion\" -o out.json"
    - "README Privacy Notes section documents PII posture (default WARNING, no cell content in logs)"
    - "README Output Shape section explicitly flags the 4 reserved placeholder keys as not-derivable from CSV"
    - "Every long flag from `python quizify_csv_ingest.py --help` appears in README.md (drift smoke test passes)"
    - "README Limitations section documents missing IDs (D-07 from Phase 2), comma-in-cell heuristic (D-05 from Phase 2), Score value string typing (D-04), --trailer-columns positional dependency"
    - "Tests in tests/test_readme_help_alignment.py verify section presence and flag/help alignment"
  artifacts:
    - path: "quizify-csv-to-json-webhook/README.md"
      provides: "Operator README per D-11 (10 sections: Purpose, Quickstart, CLI reference, Configuration table, Column assumptions, Output shape, Limitations, Privacy notes, Exit codes, Development)"
      min_lines: 80
      contains: "## Quickstart"
    - path: "quizify-csv-to-json-webhook/tests/test_readme_help_alignment.py"
      provides: "OPS-01 drift smoke test — asserts every long argparse flag is named in README.md and required sections exist"
      exports: ["test_readme_has_all_required_sections", "test_every_flag_named_in_readme"]
  key_links:
    - from: "quizify-csv-to-json-webhook/README.md"
      to: "quizify-csv-to-json-webhook/docs/webhook-quizify-format-example.json"
      via: "relative link in Output Shape section"
      pattern: "docs/webhook-quizify-format-example\\.json"
    - from: "quizify-csv-to-json-webhook/README.md"
      to: "quizify-csv-to-json-webhook/docs/quizify-submissions.csv"
      via: "relative link in Quickstart and Column assumptions sections"
      pattern: "docs/quizify-submissions\\.csv"
    - from: "tests/test_readme_help_alignment.py"
      to: "quizify_csv_ingest.py argparse parser"
      via: "subprocess `--help` capture or in-process argparse introspection"
      pattern: "subprocess\\.run\\([^)]*--help"
    - from: "tests/test_readme_help_alignment.py"
      to: "README.md"
      via: "substring match of every `--flag` in README text"
      pattern: "flag in readme"
---

<objective>
Author the operator README at `quizify-csv-to-json-webhook/README.md` per locked D-11 structure (10 sections, fixed order), and add a drift smoke test (`tests/test_readme_help_alignment.py`) that prevents future flag additions from silently desynchronizing with the docs. The README is the externally visible artifact for v1; this plan ships it.

Purpose: Close OPS-01 (concise README beside script with usage, column assumptions, limitations, privacy notes) — the final v1 milestone deliverable.

Output: A single markdown file (~120-180 lines) and one test file (~40 lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scoring-metadata-packaging/03-CONTEXT.md
@.planning/phases/03-scoring-metadata-packaging/03-RESEARCH.md
@.planning/phases/03-scoring-metadata-packaging/03-VALIDATION.md
@.planning/phases/03-scoring-metadata-packaging/03-01-SUMMARY.md
@.planning/phases/02-core-webhook-mapping/02-CONTEXT.md
@.planning/phases/01-csv-ingestion-column-layout/01-CONTEXT.md
@quizify-csv-to-json-webhook/quizify_csv_ingest.py
@quizify-csv-to-json-webhook/docs/webhook-quizify-format-example.json
@quizify-csv-to-json-webhook/docs/quizify-submissions.csv

<post_phase3_cli_surface>
<!-- Final argparse parser surface after Plan 03-01 lands.
     The README CLI reference table MUST cover every flag in this list. -->

Positional:
  csv_path                    Path to Quizify CSV export (UTF-8)

Optional flags:
  -h, --help                  argparse-builtin (do not document in CLI reference table)
  --dry-run                   Print layout summary to stderr; no JSON output (Phase 1)
  -v, --verbose               Raise log level from WARNING to INFO (Phase 1)
  --trailer-columns CSV       Override default trailer column set (Phase 1)
  -o, --output PATH           Write JSON array to PATH (UTF-8); default stdout (Phase 2)
  --emit-json                 Explicit JSON emission flag; redundant with default (Phase 2)
  --quiz-title VALUE          Quiz title; falls back to $QUIZIFY_QUIZ_TITLE then "" (Phase 3)

Env vars consumed:
  QUIZIFY_QUIZ_TITLE          Fallback for --quiz-title (Phase 3)

Exit codes:
  0  success
  1  input/layout error or any per-row skip
  2  CLI usage error
</post_phase3_cli_surface>

<configuration_table_template>
| Setting    | CLI flag        | Env var               | Default | Notes                              |
|------------|-----------------|-----------------------|---------|------------------------------------|
| Quiz title | `--quiz-title`  | `QUIZIFY_QUIZ_TITLE`  | `""`    | CLI > env > future CSV column > "" |
</configuration_table_template>

<readme_section_outline>
1. **Purpose** — one paragraph: CSV → webhook-shaped JSON; offline-first; stdlib-only.
2. **Quickstart** — one fenced code block: `python quizify_csv_ingest.py docs/quizify-submissions.csv --quiz-title "Autoevaluacion" -o out.json`. Pointer to sample fixture.
3. **CLI reference** — table or bullets covering every flag in `<post_phase3_cli_surface>` (excluding `--help`). Each entry: flag, default, description, env-var equivalent if any.
4. **Configuration table** — markdown table per `<configuration_table_template>`. Currently one row (`Quiz title`); structure ready for future rows.
5. **Column assumptions** — fixed contact prefix list (6 columns from CONTACT_PREFIX), trailer block (6 columns from DEFAULT_TRAILER), dynamic question block in between. Note the `--trailer-columns` override and that scoring/statusDate currently depend on the default trailer order (RESEARCH Pitfall 3).
6. **Output shape** — link to `docs/webhook-quizify-format-example.json`. Document key ordering per D-05. Three explicit callouts: (a) `id` is OMITTED when unknown (Phase 2 D-07); (b) `product-recommendation`, `product-link-type`, `title`, `type-page-url` are RESERVED — emitted with `null`/`""` because the CSV cannot supply them (D-02); (c) scoring keys (`result-logic`, `score-category`, `score-value`) are pass-through string verbatim from CSV (D-01, D-04).
7. **Limitations** — bullet list: missing IDs (D-07 P2), comma-in-cell answer heuristic (D-05 P2), `Yes/No → subscribed/unsubscribed` plus warn-on-other (D-11 P2), ISO-date pass-through (D-12 P2), empty rows always emit all keys (D-09 P2), Score value is string-typed (D-04 P3), `--trailer-columns` override may misalign scoring (RESEARCH Pitfall 3).
8. **Privacy notes** — exports contain PII; default log level is WARNING; warnings name columns and categorical values, never cell content (T-PII-01); `-v`/`--verbose` raises to INFO with structural detail only.
9. **Exit codes** — table or bullets: `0` success, `1` input/layout error or any per-row skip, `2` CLI usage error.
10. **Development** — `requirements-dev.txt` provides pytest tooling; runtime is stdlib-only; no `requirements.txt` (D-13). Quick test commands: `cd quizify-csv-to-json-webhook && pytest -q`.

NO worked example block (D-12). Link to fixtures instead.
</readme_section_outline>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author README.md per D-11 10-section structure</name>
  <files>quizify-csv-to-json-webhook/README.md</files>

  <read_first>
    1. `quizify-csv-to-json-webhook/quizify_csv_ingest.py` lines 15-31 (CONTACT_PREFIX, DEFAULT_TRAILER) and 348-377 (parser definitions) — copy flag names and help-text wording verbatim into the CLI reference section.
    2. `.planning/phases/03-scoring-metadata-packaging/03-01-SUMMARY.md` (written by Plan 03-01) — confirm the actual final flag list matches `<post_phase3_cli_surface>` above.
    3. `quizify-csv-to-json-webhook/docs/webhook-quizify-format-example.json` line 12 (`quiz_title`) and lines 183-186 (4 placeholders) — Output Shape section references these.
    4. `.planning/phases/03-scoring-metadata-packaging/03-CONTEXT.md` D-11 (full enumeration of section content) and D-12 (no worked example).
    5. `.planning/phases/02-core-webhook-mapping/02-CONTEXT.md` D-05, D-07, D-09, D-10..D-13 — Limitations + Privacy sections cite these.
    6. `.planning/phases/01-csv-ingestion-column-layout/01-CONTEXT.md` D-04, D-05 — Column assumptions section references the trailer block and `--trailer-columns` override.
  </read_first>

  <action>
Create `quizify-csv-to-json-webhook/README.md`. Use these exact section headings (markdown `##` level), in this exact order:

1. `## Purpose`
2. `## Quickstart`
3. `## CLI reference`
4. `## Configuration`
5. `## Column assumptions`
6. `## Output shape`
7. `## Limitations`
8. `## Privacy notes`
9. `## Exit codes`
10. `## Development`

The top-of-file H1 must be `# Quizify CSV → Webhook JSON`. The drift smoke test searches for these exact section names; do not rename.

**Section content requirements:**

**`## Purpose`** — One short paragraph. State: reads Quizify CSV exports (UTF-8), emits webhook-compatible JSON arrays matching `docs/webhook-quizify-format-example.json`. Offline-first, stdlib-only Python. Single-file CLI.

**`## Quickstart`** — One fenced bash block (verbatim, no shell prompt char):
````
```bash
python quizify_csv_ingest.py docs/quizify-submissions.csv --quiz-title "Autoevaluacion" -o out.json
```
````
Followed by one sentence: "Run from `quizify-csv-to-json-webhook/`. Sample input: [`docs/quizify-submissions.csv`](docs/quizify-submissions.csv); target shape: [`docs/webhook-quizify-format-example.json`](docs/webhook-quizify-format-example.json)."

**`## CLI reference`** — One markdown table with columns `Flag | Default | Description | Env var`. Cover every entry from `<post_phase3_cli_surface>` (excluding `--help`):
- `csv_path` (positional, no default — describe in a separate sentence above the table or as a row with `(positional)` in the Flag column)
- `--dry-run` / no default / "Print layout summary to stderr; no JSON output." / —
- `-v, --verbose` / WARNING / "Raise log level to INFO with structural detail." / —
- `--trailer-columns CSV` / `None` / "Override default trailer columns (comma-separated, ordered)." / —
- `-o, --output PATH` / stdout / "Write JSON array to PATH (UTF-8)." / —
- `--emit-json` / off / "Explicit JSON emission; redundant with default behavior." / —
- `--quiz-title VALUE` / `""` / "Quiz title; decoded via html.unescape; whitespace preserved." / `QUIZIFY_QUIZ_TITLE`

Every long flag (`--dry-run`, `--verbose`, `--trailer-columns`, `--output`, `--emit-json`, `--quiz-title`) MUST appear as a substring of the README text — the drift test scans for `f"--{long_name}"`.

**`## Configuration`** — Lead-in sentence: "Settings resolved at invocation time, in order of precedence." Then the markdown table:
````
| Setting    | CLI flag        | Env var               | Default | Notes                                |
|------------|-----------------|-----------------------|---------|--------------------------------------|
| Quiz title | `--quiz-title`  | `QUIZIFY_QUIZ_TITLE`  | `""`    | CLI > env > future CSV column > `""` |
````
Add one paragraph noting precedence resolution: CLI flag wins; env var used when flag absent; future CSV `Quiz title` column is a stub (not implemented in v1); falls back to empty string. `html.unescape` applied to the resolved value; whitespace preserved verbatim.

**`## Column assumptions`** — Three sub-bullets:
- **Contact prefix (first 6 columns):** `First name`, `Last name`, `Email`, `Lead Verified`, `Phone`, `Subscribed to newsletter`. Mismatch aborts with exit code 1.
- **Trailer block (last 6 columns by default):** `Result logic`, `Score category`, `Score value`, `Answer tags`, `Time to complete (mm:ss)`, `Date`. Override with `--trailer-columns "name1,name2,..."`. Note: scoring keys and `statusDate` currently read trailer indices 0/1/2 and 5 of the resolved trailer; `--trailer-columns` overrides that change those positions will misalign scoring fields. If you reorder the trailer, keep `Result logic`, `Score category`, `Score value`, and `Date` in the default positions.
- **Dynamic question block (everything between):** mapped in header order to `question-1` … `question-K`.

**`## Output shape`** — Lead with: "See [`docs/webhook-quizify-format-example.json`](docs/webhook-quizify-format-example.json) for the canonical target shape." Then enumerate the per-row dict key order (D-05) as a numbered list:
1. Contact + tags block: `email`, `firstName`, `lastName`, `status`, `statusDate`, `phone`, `tags`
2. `quiz_title` (resolved per Configuration)
3. Per-question triples: `question-N`, `answers-N`, `answers-tags-N` for N=1..K
4. Pass-through scoring keys: `result-logic`, `score-category`, `score-value`
5. Reserved placeholder keys: `product-recommendation`, `product-link-type`, `title`, `type-page-url`

Then three callout subsections (use `### Reserved keys`, `### Omitted keys`, `### Scoring pass-through`):
- **`### Omitted keys`** — `id` keys are omitted entirely when unknown (the CSV export does not contain answer IDs). The exporter never emits `id: null`.
- **`### Reserved keys`** — `product-recommendation`, `product-link-type`, `title`, `type-page-url` are emitted with locked defaults (`null`, `null`, `""`, `""`) because the CSV export does not contain these fields. Downstream consumers must NOT interpret `product-recommendation: null` as "Quizify said no recommendation" — it means "this exporter cannot supply this field." Fetch from the source-of-truth API if needed.
- **`### Scoring pass-through`** — `result-logic`, `score-category`, `score-value` carry the verbatim decoded strings from the CSV trailer columns (`Result logic`, `Score category`, `Score value` respectively). `Score value` is string-typed; receivers parse if numeric semantics are required.

**`## Limitations`** — Bullet list, every bullet exactly one sentence:
- Answer IDs are not present in the CSV export; the JSON omits the `id` key rather than emitting `id: null`.
- Multi-select answers detected by a comma-space heuristic (`, ` in the cell): cells matching are emitted as plain strings; otherwise as a single-element array of `{answer_name, answer_img, answer_tag}` objects.
- Subscription status mapping: `Yes` → `subscribed`, `No` or empty → `unsubscribed`. Any other value emits `unsubscribed` plus a stderr WARNING naming the unexpected categorical value.
- `statusDate` is the CSV `Date` cell verbatim. ISO `YYYY-MM-DD` shape is verified loosely; non-ISO values pass through with a WARNING (no timezone invention).
- Every row emits every key (including empty `answers-N: ""`) for stable downstream indexing.
- `Score value` is string-typed (e.g., `"500"`, `"6"`); no numeric coercion is performed.
- `--trailer-columns` override reorders the trailer block but the scoring/statusDate reads remain positional (indices 0..2 and 5); reorderings that move `Result logic`, `Score category`, `Score value`, or `Date` away from default positions will misalign those fields silently.
- HTTP POST / webhook-send mode and JSON Schema validation are deferred to v2.

**`## Privacy notes`** — Two paragraphs:
1. CSV exports contain PII (names, emails, phone numbers). The default log level is `WARNING`; emitted warnings name only column names and categorical values (e.g., `unexpected status value 'Maybe'`), never row content like email or phone. `-v`/`--verbose` raises to `INFO` with structural detail only (column counts, indices).
2. Output JSON is the operator's responsibility. Treat output files (`-o PATH`) as PII-bearing. The tool does no network I/O — output goes to stdout or the operator-named file path.

**`## Exit codes`** — Markdown table:
````
| Code | Meaning                                        |
|------|------------------------------------------------|
| 0    | Success                                        |
| 1    | Input/layout error or one or more rows skipped |
| 2    | CLI usage error (invalid flag value)           |
````

**`## Development`** — Two paragraphs:
1. Runtime is stdlib-only — there is no `requirements.txt`. Tooling (pytest) lives in `requirements-dev.txt`: `pip install -r requirements-dev.txt`. Run tests with `cd quizify-csv-to-json-webhook && pytest -q`.
2. Tests live in `tests/`. Layout, row-builder, CLI emission, structural invariants, golden-file diff, quiz-title precedence, and README/help drift are all exercised under `pytest`.

**Authoring constraints:**
- No worked CSV→JSON example block (D-12 explicitly rejects this).
- No emoji.
- Use code fences with `bash` or `text` language tags where appropriate.
- Use relative links (`docs/...`, no absolute paths).
- Single source of truth for invocation knobs: the Configuration table (D-14).
- Total length: aim for 120-180 lines. Concise > exhaustive.
  </action>

  <verify>
    <automated>test -f quizify-csv-to-json-webhook/README.md &amp;&amp; for s in "## Purpose" "## Quickstart" "## CLI reference" "## Configuration" "## Column assumptions" "## Output shape" "## Limitations" "## Privacy notes" "## Exit codes" "## Development"; do grep -qF "$s" quizify-csv-to-json-webhook/README.md || { echo "MISSING SECTION: $s"; exit 1; }; done &amp;&amp; for f in --dry-run --verbose --trailer-columns --output --emit-json --quiz-title; do grep -qF "$f" quizify-csv-to-json-webhook/README.md || { echo "MISSING FLAG: $f"; exit 1; }; done &amp;&amp; grep -qF "QUIZIFY_QUIZ_TITLE" quizify-csv-to-json-webhook/README.md &amp;&amp; grep -qF "docs/webhook-quizify-format-example.json" quizify-csv-to-json-webhook/README.md &amp;&amp; grep -qF "docs/quizify-submissions.csv" quizify-csv-to-json-webhook/README.md &amp;&amp; echo OK</automated>
    Expected: prints `OK`. Any missing section/flag/link prints a `MISSING ...` message and exits non-zero.
  </verify>

  <acceptance_criteria>
- File `quizify-csv-to-json-webhook/README.md` exists, > 80 lines
- All 10 D-11 section headings present in declared order
- All 6 long flags from `<post_phase3_cli_surface>` named in the file (substring match)
- `QUIZIFY_QUIZ_TITLE` env var named
- Relative links to both `docs/webhook-quizify-format-example.json` and `docs/quizify-submissions.csv`
- Configuration table renders as valid markdown (pipes + dashes)
- Quickstart command runs cleanly against `docs/quizify-submissions.csv` (manually spot-checked)
- No worked CSV→JSON example block (D-12)
  </acceptance_criteria>

  <done>
README authored, all sections + flags + links verified by the grep gate. Commit: `docs(phase-03-02): author operator README per D-11 ten-section structure`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add README/--help drift smoke test</name>
  <files>quizify-csv-to-json-webhook/tests/test_readme_help_alignment.py</files>

  <read_first>
    1. `quizify-csv-to-json-webhook/tests/test_cli_emit.py` (lines 1-30) — copy the `subprocess.run([sys.executable, str(SCRIPT), ...])` invocation pattern.
    2. `quizify-csv-to-json-webhook/tests/test_structural_invariants.py` (lines 1-25) — match the `ROOT = Path(__file__).resolve().parents[1]` and `SCRIPT = ROOT / "quizify_csv_ingest.py"` constants.
    3. `.planning/phases/03-scoring-metadata-packaging/03-RESEARCH.md` Pitfall 4 — drift smoke test pattern.
    4. `quizify-csv-to-json-webhook/README.md` (created in Task 1) — confirm section headings and flag names.
  </read_first>

  <action>
Create `quizify-csv-to-json-webhook/tests/test_readme_help_alignment.py` with two tests. Test names verbatim — VALIDATION.md depends on them.

```python
"""Phase 03-02 — README ↔ argparse drift smoke test (OPS-01).

Asserts (a) every required D-11 section heading is present in README.md,
(b) every long flag printed by `python quizify_csv_ingest.py --help`
appears as a substring of README.md.
"""
from __future__ import annotations

import re
import subprocess
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
SCRIPT = ROOT / "quizify_csv_ingest.py"
README = ROOT / "README.md"

REQUIRED_SECTIONS = (
    "## Purpose",
    "## Quickstart",
    "## CLI reference",
    "## Configuration",
    "## Column assumptions",
    "## Output shape",
    "## Limitations",
    "## Privacy notes",
    "## Exit codes",
    "## Development",
)


def _readme_text() -> str:
    return README.read_text(encoding="utf-8")


def _help_text() -> str:
    result = subprocess.run(
        [sys.executable, str(SCRIPT), "--help"],
        capture_output=True,
        text=True,
        check=True,
        timeout=30,
    )
    return result.stdout


def test_readme_has_all_required_sections():
    text = _readme_text()
    missing = [s for s in REQUIRED_SECTIONS if s not in text]
    assert not missing, f"README.md missing required sections: {missing}"


def test_every_flag_named_in_readme():
    help_text = _help_text()
    # Match long flags like --dry-run, --quiz-title, --trailer-columns.
    flags = set(re.findall(r"--[a-z][a-z0-9-]+", help_text))
    flags.discard("--help")  # argparse builtin; intentionally not documented.
    readme = _readme_text()
    missing = sorted(f for f in flags if f not in readme)
    assert not missing, (
        f"flags present in --help but missing from README.md: {missing}\n"
        f"Help captured:\n{help_text}"
    )
```

Do NOT add a `pytest.ini` or `conftest.py`; the existing `pytest.ini` already sets `pythonpath = .` (verified). Do NOT use watch-mode flags. Do NOT introduce dependencies beyond stdlib + pytest.
  </action>

  <verify>
    <automated>cd quizify-csv-to-json-webhook && pytest -q tests/test_readme_help_alignment.py 2>&amp;1 | tail -10</automated>
    Expected: 2 tests pass. If `test_every_flag_named_in_readme` fails, the failure message names the missing flag(s) — update README.md until green.
  </verify>

  <acceptance_criteria>
- File `quizify-csv-to-json-webhook/tests/test_readme_help_alignment.py` exists
- Both tests pass: `test_readme_has_all_required_sections` and `test_every_flag_named_in_readme`
- Test runtime < 1 second (single subprocess call)
- No external dependencies introduced (stdlib + pytest only)
- Full pytest suite remains green: `cd quizify-csv-to-json-webhook && pytest -q`
  </acceptance_criteria>

  <done>
Drift smoke test green; README docs OPS-01 fully covered. Commit: `test(phase-03-02): add README ↔ --help drift smoke test`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → README documentation | README is read-only documentation; no untrusted input flows through it |
| pytest subprocess → argparse `--help` | Test invokes the CLI with a stdlib-controlled flag; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V8-01 | Information Disclosure | README content | mitigate | README explicitly documents PII posture (default WARNING log level, no cell content in logs) per D-11.8; this is the V8 documentation control flagged by the security_enforcement carry-forward note. |
| T-REPUDIATION-01 | Repudiation | operator-vs-docs claim ("I followed the README") | mitigate | `tests/test_readme_help_alignment.py` enforces flag-name parity between `--help` output and README text; new flags cannot ship without README updates passing CI. |
| T-DOCDRIFT-01 | Tampering (silent) | future flag additions | mitigate | Same drift smoke test catches additions; section-presence test catches structural deletions. |
| T-PII-01 | Information Disclosure | (carry-forward from Phases 1-2) | mitigate | No new log surfaces in Phase 3; documentation reaffirms posture. |
</threat_model>

<verification>
- `cd quizify-csv-to-json-webhook && pytest -q` → all green (Phase 1 + 2 + 3 = ~50-60 tests in <1s)
- `cd quizify-csv-to-json-webhook && pytest -q tests/test_readme_help_alignment.py` → 2 green
- Manual review (per VALIDATION.md Manual-Only Verifications): reviewer reads `README.md` end-to-end and confirms (a) Quickstart command runs cleanly, (b) Configuration table reflects precedence accurately, (c) Limitations cover missing IDs + reserved placeholders + comma-in-cell + Score value typing, (d) Privacy Notes match Phase 2 stderr-warning posture
- Manual smoke: `python quizify_csv_ingest.py docs/quizify-submissions.csv --quiz-title "Autoevaluacion" -o /tmp/out.json && diff <(jq -S '.[0]|keys_unsorted' /tmp/out.json) <(jq -S 'keys_unsorted' docs/webhook-quizify-format-example.json)` shows only the documented `id`-absence and `product-recommendation` value differences
</verification>

<success_criteria>
- All 8 truths in `must_haves.truths` verified (6 by automated tests, 2 by structural grep gate)
- README.md exists with all 10 D-11 sections, all 6 long flags named, all required links present
- Drift smoke test passes; will fail loudly if a future flag is added without README update
- OPS-01 satisfied: README beside script with usage, column assumptions, limitations, privacy notes
- All Phase 1/2/3 tests still pass; no regressions
- Phase 3 milestone scope complete: ready for `/gsd-verify-work` then `/gsd-complete-milestone`
</success_criteria>

<output>
After completion, create `.planning/phases/03-scoring-metadata-packaging/03-02-SUMMARY.md` covering:
- README final line count
- List of section headings emitted (verify D-11 order)
- List of flags documented (verify parity with `<post_phase3_cli_surface>`)
- Test runtime added (~50-100 ms expected for the drift smoke test)
- Manual-review sign-off note (reviewer ran the Quickstart against `docs/quizify-submissions.csv` and confirmed clean exit)
- Confirmation that no `requirements.txt` was created (D-13)
- Hand-off note: phase complete; v1 milestone ready for verification + sign-off
</output>
