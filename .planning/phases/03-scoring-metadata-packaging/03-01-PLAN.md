---
phase: 03-scoring-metadata-packaging
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - quizify-csv-to-json-webhook/quizify_csv_ingest.py
  - quizify-csv-to-json-webhook/tests/test_row_builder.py
  - quizify-csv-to-json-webhook/tests/test_quiz_title_precedence.py
  - quizify-csv-to-json-webhook/tests/test_golden_structure.py
  - quizify-csv-to-json-webhook/tests/test_structural_invariants.py
autonomous: true
requirements: [WEB-04, WEB-05]
tags: [python, cli, argparse, csv, webhook]

must_haves:
  truths:
    - "build_row emits result-logic / score-category / score-value verbatim from trailer_cells_decoded[0..2]"
    - "build_row emits product-recommendation: null, product-link-type: null, title: \"\", type-page-url: \"\" on every row"
    - "Per-row dict key order matches D-05: contact(7) → quiz_title → question/answers triples → scoring(3) → placeholders(4)"
    - "--quiz-title flag value wins over $QUIZIFY_QUIZ_TITLE env var"
    - "$QUIZIFY_QUIZ_TITLE is used when --quiz-title flag is absent"
    - "quiz_title defaults to \"\" when neither CLI flag nor env var is set"
    - "html.unescape is applied to the resolved quiz_title; whitespace is preserved verbatim"
    - "Empty trailer scoring cells emit \"\" verbatim with no stderr WARNING"
  artifacts:
    - path: "quizify-csv-to-json-webhook/quizify_csv_ingest.py"
      provides: "Extended build_row/convert/main + _resolve_quiz_title helper + SCORING_PLACEHOLDERS constant + import os + --quiz-title flag"
      contains: "def _resolve_quiz_title"
    - path: "quizify-csv-to-json-webhook/tests/test_quiz_title_precedence.py"
      provides: "WEB-05 unit + subprocess tests for CLI > env > default precedence and decode/whitespace contract"
      exports: ["test_resolve_quiz_title_cli_wins", "test_resolve_quiz_title_env_used_when_flag_absent", "test_resolve_quiz_title_default_empty_when_neither", "test_resolve_quiz_title_html_unescape_applied", "test_resolve_quiz_title_whitespace_preserved", "test_subprocess_cli_overrides_env", "test_subprocess_env_used_when_no_cli", "test_subprocess_default_empty"]
    - path: "quizify-csv-to-json-webhook/tests/test_row_builder.py"
      provides: "Extended with WEB-04 unit tests for scoring pass-through + empty scoring + quiz_title parameter wiring"
      contains: "test_scoring_pass_through"
    - path: "quizify-csv-to-json-webhook/tests/test_golden_structure.py"
      provides: "PHASE_3_KEYS strip removed; key-order, scoring, placeholder, quiz_title assertions added"
      contains: "test_key_order_locked"
    - path: "quizify-csv-to-json-webhook/tests/test_structural_invariants.py"
      provides: "PHASE_3_KEYS semantics inverted from must-NOT-leak to must-be-present; 6 new invariant tests added"
      contains: "test_every_row_has_quiz_title"
  key_links:
    - from: "quizify_csv_ingest.py::main"
      to: "quizify_csv_ingest.py::_resolve_quiz_title"
      via: "called once after parser.parse_args(argv) with (args, os.environ)"
      pattern: "_resolve_quiz_title\\(args,\\s*os\\.environ\\)"
    - from: "quizify_csv_ingest.py::main"
      to: "quizify_csv_ingest.py::convert"
      via: "passes resolved quiz_title as 4th positional arg"
      pattern: "convert\\([^)]*quiz_title"
    - from: "quizify_csv_ingest.py::convert"
      to: "quizify_csv_ingest.py::build_row"
      via: "threads quiz_title into each build_row call"
      pattern: "build_row\\([^)]*quiz_title"
    - from: "quizify_csv_ingest.py::build_row"
      to: "row dict keys"
      via: "row[\"quiz_title\"] = quiz_title at position 8; row[\"result-logic\"]/[\"score-category\"]/[\"score-value\"] from trailer[0..2]; 4 placeholder keys at end"
      pattern: "row\\[\"result-logic\"\\]\\s*=\\s*trailer_cells_decoded\\[0\\]"
---

<objective>
Extend `quizify_csv_ingest.py` so each emitted row carries `quiz_title` (resolved from `--quiz-title` flag → `$QUIZIFY_QUIZ_TITLE` env var → default `""`), three pass-through scoring keys (`result-logic`, `score-category`, `score-value`) sourced verbatim from `trailer_cells_decoded[0..2]`, and four reserved placeholder keys (`product-recommendation: null`, `product-link-type: null`, `title: ""`, `type-page-url: ""`). All keys emitted in the locked D-05 order. TDD: tests first (RED), implementation second (GREEN), then refactor scaffolding test files (GREEN preserved).

Purpose: Close WEB-04 (scoring pass-through + recommendation slots) and WEB-05 (quiz title precedence) — the last two code-side requirements before the v1 milestone ships.

Output: A single-file CLI extension (no new modules), 11 new keys per row, ~15 new tests, RED→GREEN-verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scoring-metadata-packaging/03-CONTEXT.md
@.planning/phases/03-scoring-metadata-packaging/03-RESEARCH.md
@.planning/phases/03-scoring-metadata-packaging/03-VALIDATION.md
@.planning/phases/02-core-webhook-mapping/02-CONTEXT.md
@.planning/phases/01-csv-ingestion-column-layout/01-CONTEXT.md
@quizify-csv-to-json-webhook/quizify_csv_ingest.py
@quizify-csv-to-json-webhook/docs/webhook-quizify-format-example.json
@quizify-csv-to-json-webhook/docs/quizify-submissions.csv
@quizify-csv-to-json-webhook/tests/test_row_builder.py
@quizify-csv-to-json-webhook/tests/test_golden_structure.py
@quizify-csv-to-json-webhook/tests/test_structural_invariants.py
@quizify-csv-to-json-webhook/tests/test_cli_emit.py
@quizify-csv-to-json-webhook/tests/conftest.py
@quizify-csv-to-json-webhook/pytest.ini
@quizify-csv-to-json-webhook/requirements-dev.txt

<interfaces>
<!-- Current quizify_csv_ingest.py contracts the executor extends. Verbatim from code; no exploration needed. -->

# Module-level constants (line 15-31, 80-84):
CONTACT_PREFIX = ("First name", "Last name", "Email", "Lead Verified", "Phone", "Subscribed to newsletter")
DEFAULT_TRAILER = ("Result logic", "Score category", "Score value", "Answer tags", "Time to complete (mm:ss)", "Date")
TAG_HEADER_MAP = {"red_flag": "signos de alarma", "goal_": "objetivo", "consent": "consiento"}

# Existing functions (line numbers from current file):
# line 87:  def decode_cell(s: str) -> str
# line 92:  def shape_answer(decoded: str)
# line 105: def map_status(raw: str) -> tuple[str, str | None]
# line 136: def match_tags_to_questions(tag_csv, dynamic_headers_decoded) -> tuple[dict[int, list[str]], list[str]]

# build_row signature (line 172-177) — Phase 3 adds `quiz_title: str` parameter:
def build_row(
    prefix_cells_decoded: list[str],
    dynamic_cells_decoded: list[str],
    trailer_cells_decoded: list[str],
    dynamic_headers_decoded: list[str],
) -> tuple[dict, list[str]]: ...

# convert signature (line 272-276) — Phase 3 adds `quiz_title: str` parameter:
def convert(path: Path, trailer: tuple[str, ...] | None, output: Path | None) -> int: ...

# main entrypoint (line 348-377) — Phase 3 adds --quiz-title argparse arg + _resolve_quiz_title call:
def main(argv: list[str] | None = None) -> int: ...

# Current build_row row dict assembly (line 216-230) — Phase 3 inserts quiz_title at position 8 and appends 7 keys after the question loop.
</interfaces>

<scoring_placeholders_constant>
# Recommended module-level constant (placed after TAG_HEADER_MAP, ~line 85):
SCORING_PLACEHOLDERS = {
    "product-recommendation": None,
    "product-link-type": None,
    "title": "",
    "type-page-url": "",
}
</scoring_placeholders_constant>

<resolve_helper_signature>
# New private helper (placed near map_status, before build_row):
def _resolve_quiz_title(args: argparse.Namespace, environ: "os._Environ[str] | dict[str, str]") -> str:
    """D-07 precedence: CLI flag > env var > "" (future CSV column stub).

    Decodes via html.unescape at the boundary (D-09); whitespace preserved (no .strip()).
    """
    if args.quiz_title is not None:
        return html.unescape(args.quiz_title)
    env_val = environ.get("QUIZIFY_QUIZ_TITLE")
    if env_val is not None:
        return html.unescape(env_val)
    # Future: CSV "Quiz title" column lookup goes here (D-07 stub; do NOT invent the header name).
    return ""
</resolve_helper_signature>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing Wave-0 tests for scoring + placeholders + quiz_title (RED)</name>
  <files>
    quizify-csv-to-json-webhook/tests/test_quiz_title_precedence.py,
    quizify-csv-to-json-webhook/tests/test_row_builder.py,
    quizify-csv-to-json-webhook/tests/test_golden_structure.py,
    quizify-csv-to-json-webhook/tests/test_structural_invariants.py
  </files>

  <read_first>
    Read these in order before writing any test code:
    1. `quizify-csv-to-json-webhook/tests/test_row_builder.py` — match its existing import style, fixture conventions, and `_decoded(...)` helper if any. Add new tests in the same module.
    2. `quizify-csv-to-json-webhook/tests/test_structural_invariants.py` (lines 1-100) — note `EXPECTED_K = 20`, the module-scoped `emitted_payload` fixture, and the current `PHASE_3_KEYS` frozenset (line 27-36) which currently asserts must-NOT-leak. You will invert the semantic.
    3. `quizify-csv-to-json-webhook/tests/test_golden_structure.py` (full) — find the `PHASE_3_KEYS` strip filter (line 28-36), `_build_aligned_csv` row builder, and `run_aligned` subprocess invoker. You will (a) drop the strip, (b) add scoring trailer values to `_build_aligned_csv`, (c) add `--quiz-title "Autoevaluacion"` to `run_aligned`'s argv.
    4. `quizify-csv-to-json-webhook/tests/test_cli_emit.py` (lines 1-30) — copy its `subprocess.run([sys.executable, str(SCRIPT), ...], capture_output=True, text=True, check=False)` pattern for the new precedence subprocess tests.
    5. `quizify-csv-to-json-webhook/docs/webhook-quizify-format-example.json` line 12 (`"quiz_title": "Autoevaluacion"`) and lines 183-186 (the four placeholder keys with example values).
    6. `.planning/phases/03-scoring-metadata-packaging/03-VALIDATION.md` Per-Task Verification Map — every test name listed for tasks 03-01-02..03-01-06 MUST appear in your test files with that exact name.
  </read_first>

  <action>
This is a TDD RED step. Write the test scaffolding so it fails for the right reason (the production code does not yet emit the new keys / has no `--quiz-title` flag). Per D-15, env resolution happens in `main()` after `parse_args` via `_resolve_quiz_title(args, os.environ)`.

**A. CREATE `quizify-csv-to-json-webhook/tests/test_quiz_title_precedence.py`** with these tests (test names verbatim — VALIDATION.md depends on them):

Imports: `import argparse, os, subprocess, sys, json, html` from stdlib; `from pathlib import Path`; `import pytest`. Module constants: `ROOT = Path(__file__).resolve().parents[1]`, `SCRIPT = ROOT / "quizify_csv_ingest.py"`. Import the helper for unit tests: `sys.path.insert(0, str(ROOT))` then `from quizify_csv_ingest import _resolve_quiz_title`.

Helper: `_make_minimal_csv(tmp_path) -> Path` — write a CSV with the canonical header (CONTACT_PREFIX + one dynamic question `"Q1?"` + DEFAULT_TRAILER) and a single data row whose subscribed cell is `"Yes"`, date is `"2024-01-15"`, and trailer cells are non-empty (e.g. `Score / Test / 100 / "" / 00:30 / 2024-01-15`). Use `csv.writer` with `newline=""`.

Tests:
- `def test_resolve_quiz_title_cli_wins():` — `ns = argparse.Namespace(quiz_title="FromCli"); assert _resolve_quiz_title(ns, {"QUIZIFY_QUIZ_TITLE": "FromEnv"}) == "FromCli"`.
- `def test_resolve_quiz_title_env_used_when_flag_absent():` — `ns = argparse.Namespace(quiz_title=None); assert _resolve_quiz_title(ns, {"QUIZIFY_QUIZ_TITLE": "FromEnv"}) == "FromEnv"`.
- `def test_resolve_quiz_title_default_empty_when_neither():` — `ns = argparse.Namespace(quiz_title=None); assert _resolve_quiz_title(ns, {}) == ""`.
- `def test_resolve_quiz_title_html_unescape_applied():` — `ns = argparse.Namespace(quiz_title="Salud &amp; Bienestar"); assert _resolve_quiz_title(ns, {}) == "Salud & Bienestar"`. Also assert env path: `ns2 = argparse.Namespace(quiz_title=None); assert _resolve_quiz_title(ns2, {"QUIZIFY_QUIZ_TITLE": "&lt;tag&gt;"}) == "<tag>"`.
- `def test_resolve_quiz_title_whitespace_preserved():` — `ns = argparse.Namespace(quiz_title="  Padded  "); assert _resolve_quiz_title(ns, {}) == "  Padded  "` (D-09: do NOT `.strip()`).
- `def test_subprocess_cli_overrides_env(tmp_path):` — invoke `[sys.executable, str(SCRIPT), str(csv), "--quiz-title", "FromCli"]` with `env={**os.environ, "QUIZIFY_QUIZ_TITLE": "FromEnv"}`, parse stdout, assert `payload[0]["quiz_title"] == "FromCli"`.
- `def test_subprocess_env_used_when_no_cli(tmp_path):` — same invocation without `--quiz-title`, assert `payload[0]["quiz_title"] == "FromEnv"`.
- `def test_subprocess_default_empty(tmp_path):` — invoke with `env = {k: v for k, v in os.environ.items() if k != "QUIZIFY_QUIZ_TITLE"}`, assert `payload[0]["quiz_title"] == ""`.

For all subprocess tests use `subprocess.run(..., capture_output=True, text=True, check=False, timeout=60)` and assert `result.returncode == 0` before parsing JSON. Build the env dict explicitly so a developer's exported `QUIZIFY_QUIZ_TITLE` does not leak into the default-empty test.

**B. EXTEND `quizify-csv-to-json-webhook/tests/test_row_builder.py`** by appending these tests (do not delete existing tests):

- `def test_quiz_title_threaded_through_build_row():` — call `build_row(prefix_d, dynamic_d, trailer_d, headers_d, quiz_title="Autoevaluacion")`. Assert `row["quiz_title"] == "Autoevaluacion"` and `list(row.keys())[7] == "quiz_title"` (0-indexed position 7 = 8th key per D-05).
- `def test_scoring_pass_through():` — synthesize `trailer_d = ["Score", "Signos de Alarma", "500", "", "00:30", "2024-01-15"]`. Call `build_row(...)` with any valid prefix/dynamic. Assert `row["result-logic"] == "Score"`, `row["score-category"] == "Signos de Alarma"`, `row["score-value"] == "500"` (string verbatim per D-04, NOT int).
- `def test_empty_scoring_emits_empty_strings():` — `trailer_d = ["", "", "", "", "", "2024-01-15"]`. Assert `row["result-logic"] == ""`, `row["score-category"] == ""`, `row["score-value"] == ""` and that no warning was returned in `warnings_out` for the empty scoring (D-03).
- `def test_reserved_placeholders_match_locked_defaults():` — assert `row["product-recommendation"] is None`, `row["product-link-type"] is None`, `row["title"] == ""`, `row["type-page-url"] == ""` (D-02 verbatim).
- `def test_key_order_matches_d05():` — assert `list(row.keys())[:8] == ["email", "firstName", "lastName", "status", "statusDate", "phone", "tags", "quiz_title"]` and `list(row.keys())[-7:] == ["result-logic", "score-category", "score-value", "product-recommendation", "product-link-type", "title", "type-page-url"]`.

You may need to update existing test_row_builder.py call sites that invoke `build_row` to pass `quiz_title=""` (keyword arg) so existing Phase 2 tests still pass once the parameter is added in Task 2. Do this in Task 1 RED — the tests should still fail because the new keys don't exist yet, NOT because of TypeError on the signature.

**C. EXTEND `quizify-csv-to-json-webhook/tests/test_golden_structure.py`:**
- DELETE the `PHASE_3_KEYS` strip from the example-key-set comparison (lines 28-36 frozenset and any `- PHASE_3_KEYS` set arithmetic). The aligned row should now match the example top-level key set exactly except for `id`-keys per Phase 2 D-07.
- In `_build_aligned_csv` (locate the row-list construction near lines 116-123 per RESEARCH.md), set `Result logic = "Score"`, `Score category = "Signos de Alarma"`, `Score value = "500"` (no longer `""`).
- In `run_aligned` (the subprocess invoker), append `"--quiz-title", "Autoevaluacion"` to the argv list so the aligned row's `quiz_title` matches the example's value.
- ADD `def test_key_order_locked(...)`: assert the emitted row's `list(keys())[7] == "quiz_title"` and `list(keys())[-7:] == ["result-logic", "score-category", "score-value", "product-recommendation", "product-link-type", "title", "type-page-url"]`.
- ADD `def test_scoring_keys_present_after_phase3(...)`: assert pass-through values match the trailer cells set above.
- ADD `def test_reserved_placeholders_match_defaults(...)`: assert `row["product-recommendation"] is None`, `row["product-link-type"] is None`, `row["title"] == ""`, `row["type-page-url"] == ""`.

**D. EXTEND `quizify-csv-to-json-webhook/tests/test_structural_invariants.py`:**
- Rename the existing `PHASE_3_KEYS` constant to `PHASE_3_REQUIRED_KEYS = frozenset({"quiz_title", "product-recommendation", "product-link-type", "title", "type-page-url", "result-logic", "score-category", "score-value"})` (now 8 keys, all REQUIRED).
- Update or replace the existing "must NOT leak" test with `def test_every_row_has_phase3_required_keys(emitted_payload):` asserting every row has every key in `PHASE_3_REQUIRED_KEYS`.
- ADD `def test_every_row_has_quiz_title(emitted_payload):` — `assert "quiz_title" in row and isinstance(row["quiz_title"], str)`.
- ADD `def test_every_row_has_scoring_keys(emitted_payload):` — assert `result-logic`, `score-category`, `score-value` are all present and string-typed.
- ADD `def test_every_row_has_reserved_placeholders(emitted_payload):` — assert `row["product-recommendation"] is None`, `row["product-link-type"] is None`, `row["title"] == ""`, `row["type-page-url"] == ""`.
- ADD `def test_key_order_locked(emitted_payload):` — for each row, assert `list(row.keys())[7] == "quiz_title"` and `list(row.keys())[-7:] == ["result-logic", "score-category", "score-value", "product-recommendation", "product-link-type", "title", "type-page-url"]`.
- ADD `def test_quiz_title_default_empty_when_no_flag_or_env(tmp_path):` — re-invoke the CLI in a separate (function-scoped) subprocess with `env` explicitly excluding `QUIZIFY_QUIZ_TITLE`, assert every row's `quiz_title == ""`. (The module-scoped fixture also runs without the flag — but module fixtures inherit ambient env. This dedicated test pins the contract.)

Do NOT modify `quizify_csv_ingest.py` in this task. Tests must fail RED with messages like `KeyError: 'quiz_title'`, `TypeError: build_row() got an unexpected keyword argument 'quiz_title'`, or assertion failures on missing keys.
  </action>

  <verify>
    <automated>cd quizify-csv-to-json-webhook && pytest -q tests/test_quiz_title_precedence.py tests/test_row_builder.py tests/test_golden_structure.py tests/test_structural_invariants.py 2>&amp;1 | tail -40</automated>
    Expected outcome: tests FAIL with errors referencing the missing `quiz_title` key, missing `_resolve_quiz_title` import, missing `--quiz-title` argument, or missing scoring/placeholder keys. Failures with these signatures = correct RED. Failures from syntax errors / import errors in the test files themselves = wrong; fix before continuing.
  </verify>

  <acceptance_criteria>
- File `quizify-csv-to-json-webhook/tests/test_quiz_title_precedence.py` exists with the 8 named tests above
- `tests/test_row_builder.py` has 5 new tests added without deleting Phase 2 tests
- `tests/test_golden_structure.py` has `PHASE_3_KEYS` strip removed, `_build_aligned_csv` populates scoring cells, `run_aligned` passes `--quiz-title "Autoevaluacion"`, and 3 new tests added
- `tests/test_structural_invariants.py` has `PHASE_3_KEYS` semantics inverted to required, with 6 new tests added
- Running pytest fails with errors clearly attributable to missing production code (not test syntax errors)
- Every test name listed in `03-VALIDATION.md` Per-Task Verification Map exists with that exact name
  </acceptance_criteria>

  <done>
Tests written, RED confirmed (pytest exits non-zero with phase-3-key-related failures), no production code touched yet. Commit: `test(phase-03-01): add failing tests for scoring + placeholders + quiz_title precedence`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scoring keys, placeholders, and --quiz-title (GREEN)</name>
  <files>quizify-csv-to-json-webhook/quizify_csv_ingest.py</files>

  <read_first>
    1. `quizify-csv-to-json-webhook/quizify_csv_ingest.py` (full file — already in context). Identify the exact insertion points: line 11 (after `import sys`), after line 84 (`TAG_HEADER_MAP` block), line 117 (after `map_status` ends), line 172-177 (`build_row` signature), line 224 (between `"tags": tags_list,` and the dynamic loop — actually positions 7→8), line 230 (after the `for i, header...` loop ends), line 272-276 (`convert` signature), line 329-331 (`build_row` call site inside `convert`), line 357 (after `--emit-json` add_argument), line 362 (after `args = parser.parse_args(argv)`), line 377 (`return convert(...)` call site).
    2. `.planning/phases/03-scoring-metadata-packaging/03-RESEARCH.md` Code Examples section — copy the `_resolve_quiz_title` body and the build_row insertion shape verbatim. RESEARCH locks the bounds-checked indexing pattern: `trailer_cells_decoded[0] if len(trailer_cells_decoded) > 0 else ""` (and similarly for [1], [2]).
    3. CONTEXT.md D-01..D-09, D-15..D-17 (already in context) — do not re-decide.
  </read_first>

  <action>
This is the TDD GREEN step. Make minimal edits to `quizify_csv_ingest.py` so the Task-1 tests pass. Do not add functionality the tests don't exercise. No new modules, no refactor of unrelated code.

Apply these edits in order:

**1. Imports (line 11 region).** Insert `import os` after `import logging` (line 10) — alphabetical order keeps `os` before `sys`. Final import block top-to-bottom: `argparse, csv, html, json, logging, os, sys, unicodedata`.

**2. Module-level constant** (after `TAG_HEADER_MAP` block, after line 84). Insert:
```python
SCORING_PLACEHOLDERS = {
    "product-recommendation": None,
    "product-link-type": None,
    "title": "",
    "type-page-url": "",
}
```

**3. `_resolve_quiz_title` helper** (insert after `map_status`, before `_norm_for_match`, ~line 117). Verbatim from `<resolve_helper_signature>` in the context block:
```python
def _resolve_quiz_title(args: argparse.Namespace, environ) -> str:
    """D-07 precedence: CLI flag > env var > "" (future CSV column stub).

    Decodes via html.unescape at the boundary (D-09); whitespace preserved.
    """
    if args.quiz_title is not None:
        return html.unescape(args.quiz_title)
    env_val = environ.get("QUIZIFY_QUIZ_TITLE")
    if env_val is not None:
        return html.unescape(env_val)
    return ""
```
Use a permissive type for `environ` (no annotation, or `"os._Environ[str] | dict[str, str]"` as a string annotation) so unit tests can pass plain dicts.

**4. Extend `build_row` signature** (line 172-177). Add `quiz_title: str` as the 5th positional parameter:
```python
def build_row(
    prefix_cells_decoded: list[str],
    dynamic_cells_decoded: list[str],
    trailer_cells_decoded: list[str],
    dynamic_headers_decoded: list[str],
    quiz_title: str,
) -> tuple[dict, list[str]]:
```

**5. Insert `quiz_title` into the row dict** (between current line 223 `"tags": tags_list,` and line 224 closing brace of the dict literal — i.e., make `quiz_title` the 8th key per D-05):
```python
    row: dict = {
        "email": email,
        "firstName": first_name,
        "lastName": last_name,
        "status": status_value,
        "statusDate": status_date,
        "phone": phone,
        "tags": tags_list,
        "quiz_title": quiz_title,
    }
```

**6. Append scoring + placeholder keys** (after the `for i, header in enumerate(...)` loop ends — currently line 230, before `return row, warnings_out` on line 231):
```python
    row["result-logic"]   = trailer_cells_decoded[0] if len(trailer_cells_decoded) > 0 else ""
    row["score-category"] = trailer_cells_decoded[1] if len(trailer_cells_decoded) > 1 else ""
    row["score-value"]    = trailer_cells_decoded[2] if len(trailer_cells_decoded) > 2 else ""
    row.update(SCORING_PLACEHOLDERS)
```
The `row.update(SCORING_PLACEHOLDERS)` line preserves dict insertion order in Python 3.7+ — keys are appended in the constant's declared order: product-recommendation, product-link-type, title, type-page-url (matches D-05 final tail).

**7. Extend `convert` signature** (line 272-276). Add `quiz_title: str` parameter:
```python
def convert(
    path: Path,
    trailer: tuple[str, ...] | None,
    output: Path | None,
    quiz_title: str,
) -> int:
```

**8. Pass `quiz_title` into `build_row`** at the call site (line 329-331):
```python
            row_dict, warnings_out = build_row(
                prefix_d, dynamic_d, trailer_d, dynamic_headers_decoded, quiz_title
            )
```

**9. Add `--quiz-title` argparse argument** (in `main`, after the `--emit-json` add_argument near line 357-361). Insert exactly:
```python
    parser.add_argument(
        "--quiz-title",
        default=None,
        help='Quiz title; falls back to $QUIZIFY_QUIZ_TITLE env var, then "". Decoded via html.unescape.',
    )
```
**Critical:** `default=None` (not `os.environ.get(...)`) — see RESEARCH Pitfall 1. The `default=None` form is what makes `_resolve_quiz_title` testable in-process.

**10. Resolve env var precedence** (in `main`, after `args = parser.parse_args(argv)` line 362, before the `trailer_override` block at line 364):
```python
    quiz_title = _resolve_quiz_title(args, os.environ)
```

**11. Pass `quiz_title` into `convert`** at the call site (line 377):
```python
    return convert(args.csv_path, trailer_override, args.output, quiz_title)
```

Do NOT modify `dry_run`, `classify_headers`, `decode_cell`, `shape_answer`, `map_status`, `match_tags_to_questions`, `parse_trailer_arg`, `configure_logging`, or any Phase 1/2 logic. Do NOT introduce a new module file. Do NOT add `--product-recommendation` or other placeholder-override flags (D-17 forbids them). Do NOT log a warning for empty scoring cells (D-03).

After edits, the file should compile and `python -c "import quizify_csv_ingest"` should succeed.
  </action>

  <verify>
    <automated>cd quizify-csv-to-json-webhook && python -c "import quizify_csv_ingest; assert hasattr(quizify_csv_ingest, '_resolve_quiz_title'); assert hasattr(quizify_csv_ingest, 'SCORING_PLACEHOLDERS')" &amp;&amp; pytest -q tests/test_quiz_title_precedence.py tests/test_row_builder.py tests/test_golden_structure.py tests/test_structural_invariants.py 2>&amp;1 | tail -20</automated>
    Expected: import smoke check passes; pytest exits 0 with all Task-1 tests now GREEN.
  </verify>

  <acceptance_criteria>
- `quizify_csv_ingest.py` imports `os` and exposes `SCORING_PLACEHOLDERS` and `_resolve_quiz_title`
- `build_row` accepts `quiz_title` as 5th positional parameter; emits `quiz_title` at row position 8 (0-indexed: 7) and scoring/placeholder keys at positions -7..-1 in declared D-05 order
- `convert` accepts `quiz_title` 4th positional parameter; threads it into every `build_row` call
- `main` exposes `--quiz-title` argparse flag with `default=None` and resolves env var via `_resolve_quiz_title(args, os.environ)` after `parse_args`
- All Task-1 tests pass (pytest exits 0 on the four test files)
- No Phase 1 / Phase 2 tests regress (`pytest -q` full suite green)
- No new third-party imports introduced; only `os` from stdlib
- No WARNING logged for empty scoring cells; whitespace preserved in `--quiz-title` value
  </acceptance_criteria>

  <done>
Code edits complete, full pytest suite green, key order verified, precedence (CLI > env > default `""`) verified by subprocess tests. Commit: `feat(phase-03-01): emit scoring + placeholder keys and --quiz-title precedence`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → `argv` | `--quiz-title` value comes from operator's shell; not external input |
| operator-shell → `os.environ` | `$QUIZIFY_QUIZ_TITLE` is operator-controlled |
| CSV file → process | unchanged from Phase 2 (already mitigated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PII-01 | Information Disclosure | log surfaces in `convert`/`build_row` | mitigate (carry-forward from Phase 2) | No new log sites added in Phase 3 (D-03 forbids warning on empty scoring); `quiz_title` value is never logged. |
| T-V5-01 | Tampering | `--quiz-title` flag value | accept | Operator owns the shell. Decode via `html.unescape` at the boundary (D-09) prevents `&amp;`-style downstream re-rendering; JSON serialization (`json.dump` with `ensure_ascii=False`) handles JSON escaping. No SQL/HTML/JS injection sinks (output is JSON consumed by webhook). |
| T-V5-02 | Tampering | `$QUIZIFY_QUIZ_TITLE` env var | accept | Same rationale as T-V5-01: operator-controlled, decoded at boundary, no injection sinks. |
| T-V12-01 | Information Disclosure | env-var read | accept | `os.environ.get(...)` reads only the named key; no environ enumeration; value path is identical to flag path. |
</threat_model>

<verification>
- Full pytest suite green: `cd quizify-csv-to-json-webhook && pytest -q`
- Manual smoke: `python quizify_csv_ingest.py docs/quizify-submissions.csv --quiz-title "Autoevaluacion" -o /tmp/out.json && jq '.[0] | keys_unsorted | .[7]' /tmp/out.json` returns `"quiz_title"`
- Manual smoke: `jq '.[0] | keys_unsorted | .[-7:]' /tmp/out.json` returns the 7 final keys in D-05 order
- Manual smoke: `QUIZIFY_QUIZ_TITLE="EnvWins" python quizify_csv_ingest.py docs/quizify-submissions.csv | jq '.[0]."quiz_title"'` returns `"EnvWins"`
- Manual smoke: `python quizify_csv_ingest.py docs/quizify-submissions.csv --quiz-title 'CliWins' | env QUIZIFY_QUIZ_TITLE='EnvLoses' xargs -I{} jq '.[0]."quiz_title"' <(python quizify_csv_ingest.py docs/quizify-submissions.csv --quiz-title 'CliWins')` returns `"CliWins"` (CLI overrides env when both set)
</verification>

<success_criteria>
- All 8 truths in `must_haves.truths` verified by automated tests
- All artifacts in `must_haves.artifacts` exist and pass their `contains` / `exports` constraints
- WEB-04 satisfied: scoring pass-through emits verbatim values; placeholders match locked defaults; key order matches D-05
- WEB-05 satisfied: `--quiz-title` precedence (CLI > env > `""`) verified at unit + subprocess tier; `html.unescape` applied; whitespace preserved
- No regressions in Phase 1 / Phase 2 tests
- Total Phase 3 added test runtime < 1 second (per RESEARCH Test Strategy budget)
</success_criteria>

<output>
After completion, create `.planning/phases/03-scoring-metadata-packaging/03-01-SUMMARY.md` covering:
- Files modified (with line-count delta)
- Tests added (count by file)
- Test runtime delta (Phase 2 baseline → Phase 3 total)
- Any deviations from CONTEXT.md decisions (expected: none)
- Confirmation that `import os` is the only new stdlib addition
- Hand-off note for Plan 03-02: the actual final list of CLI flags after Phase 3 lands (so README CLI reference + drift smoke test work against ground truth)
</output>
