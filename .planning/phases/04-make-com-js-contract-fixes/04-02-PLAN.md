---
phase: 04-make-com-js-contract-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - quizify-csv-to-json-webhook/make-scripts/CONVENTIONS.md
autonomous: true
requirements:
  - MAKE-FIX-03
must_haves:
  truths:
    - "T-PII-01 is preserved in CONVENTIONS.md: only row indices + categorical enum values are cited; no email, phone, or full-name PII appears as example data (D-04-04)."
    - "The four mandated topics from MAKE-FIX-03 acceptance criterion are all covered in a single ~50–80 line file (D-04-04)."
    - "CONTRACT-01 verification uses an inline synthetic JSON fixture (test@example.com placeholder) rather than instructing CSV row hand-edits (Pitfall #12 + T-PII-01)."
    - "MAKE-FIX-02 verification acknowledges the 42-row sample contains zero athlete rows; athlete branch requires a synthetic Module 2 input."
  artifacts:
    - path: "quizify-csv-to-json-webhook/make-scripts/CONVENTIONS.md"
      provides: "Verification documentation for CONTRACT-01, MAKE-FIX-01, MAKE-FIX-02 + tag canonical-spelling rule"
      contains: "snake_case"
      min_lines: 50
  key_links:
    - from: "CONVENTIONS.md §MAKE-FIX-01 verification"
      to: "docs/quizify-submissions.csv rows 10 and 35"
      via: "row-index reference (Karen Retamal, Javielys Mancilla as identifiers only)"
      pattern: "row 10"
    - from: "CONVENTIONS.md §CONTRACT-01 verification"
      to: "Make.com Module 1 test interface"
      via: "inline synthetic JSON fixture (no real CSV edit)"
      pattern: "product-recommendation"
---

<objective>
Document the manual verification approach for the three JS fixes landed in Plan 01, plus codify the snake_case tag-spelling convention as a written rule for `make-scripts/`. This is the documentation half of Option B (one bundled JS commit + one docs commit, per ROADMAP "single-pass review" rationale and CONTEXT.md "Claude's Discretion"). CONVENTIONS.md exists nowhere today; this plan creates it as a new file.

Purpose: Give the operator a checkable verification recipe for each of the three Plan-01 fixes — and a single canonical reference for the snake_case tag rule so future contributors don't reintroduce hyphen outliers.
Output: One new ~50–80 line Markdown file at `quizify-csv-to-json-webhook/make-scripts/CONVENTIONS.md`.

**Why Wave 2:** This plan depends on Plan 01 because the verification examples reference the post-fix textual state (`hasTag(tags, "peri_menu")`, `if (data.is_athlete)`, no `product_result` key). Writing these examples before the JS edits would invert the source-of-truth direction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-make-com-js-contract-fixes/04-CONTEXT.md
@.planning/phases/04-make-com-js-contract-fixes/04-RESEARCH.md
@.planning/phases/04-make-com-js-contract-fixes/04-PATTERNS.md
@.planning/phases/04-make-com-js-contract-fixes/04-VALIDATION.md
@.planning/phases/04-make-com-js-contract-fixes/04-01-SUMMARY.md
@quizify-csv-to-json-webhook/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MAKE-FIX-03 — Create make-scripts/CONVENTIONS.md with the four mandated topics</name>
  <files>quizify-csv-to-json-webhook/make-scripts/CONVENTIONS.md</files>
  <read_first>
    - quizify-csv-to-json-webhook/README.md (style analog: H2-only, noun-phrase titles, fenced bash/json blocks, em-dash for empty cells)
    - .planning/phases/04-make-com-js-contract-fixes/04-CONTEXT.md (D-04-04: scope locked to four topics, ~50–80 lines, no architecture explainer)
    - .planning/phases/04-make-com-js-contract-fixes/04-RESEARCH.md (Pitfall #12 caveat with synthetic JSON fixture; row 10 / row 35 / row 5 confirmations; T-PII-01 PII rules)
    - .planning/phases/04-make-com-js-contract-fixes/04-PATTERNS.md (mandated content map → recommended section titles; tone rules)
    - .planning/phases/04-make-com-js-contract-fixes/04-01-SUMMARY.md (post-fix file state — CONVENTIONS.md examples must match)
  </read_first>
  <action>
    Create the new file `quizify-csv-to-json-webhook/make-scripts/CONVENTIONS.md` covering exactly the four topics from D-04-04, in ~50–80 lines, using the README.md style (H2-only, noun-phrase titles, ```bash``` and ```json``` fenced blocks, em-dash for empty cells).

    **Required sections** (use these titles verbatim per Pattern Map "Mandated content map"):

    1. `## Tag naming convention` — State the rule: all tag identifiers in `make-scripts/` use `snake_case_underscores`. List the canonical set: `has_red_flags`, `has_triggers`, `has_limitations`, `has_pelvic_symptoms`, `is_athlete`, `hogar`, `menstrual`, `postpartum`, `peri_menu`, `consent_given`. State that the consumer side (`score-calculations.js` `hasTag` calls) must match the emitter spelling exactly (case-sensitive `tags.includes` check). Include a one-line note: "score_total (JS-recomputed in `score-calculations.js`) and `score-value` (Python CSV pass-through) are independent values; a post-v1.1 audit should confirm agreement" (Pitfall #17).

    2. `## CONTRACT-01 verification` — Explain that the canonical key in the Python emit is `product-recommendation` (hyphenated, per D-05) and Module 1 reads it as `record["product-recommendation"]`. Note that `docs/quizify-submissions.csv` does NOT contain a `product-recommendation` column (per Pitfall #12), so verification requires a synthetic inline-JSON fixture pasted into Make.com's module test interface. Include this exact code block:

    ```json
    {
      "product-recommendation": "programa-piso-pelvico",
      "email": "test@example.com",
      "firstName": "Test",
      "lastName": "User",
      "phone": "",
      "status": "",
      "statusDate": "",
      "quiz_title": "quizify"
    }
    ```

    Then state: "Confirm Module 1 output exposes `product_recommendation: \"programa-piso-pelvico\"` and does NOT contain a `product_result` key." Use only the placeholder PII shown above — do NOT instruct adding fake PII rows to the real CSV (T-PII-01).

    3. `## MAKE-FIX-01 verification` — Use a small pipe-table identifying the verification rows by index only:

    | Row | Column | Value |
    |-----|--------|-------|
    | row 10 | `menopause_status` | `Perimenopausia` |
    | row 35 | `menopause_status` | `Perimenopausia` |

    Then describe: deploy the updated `score-calculations.js`; run the Make.com scenario against rows 10 and 35; confirm Module 2 output has `is_peri_meno: true` and `life_stage` includes `peri_menopause_menopause`. Names (Karen Retamal, Javielys Mancilla) MAY be cited as row identifiers in a single sentence ("Rows 10 and 35 correspond to respondents Karen Retamal and Javielys Mancilla per CONTEXT.md") — but no other PII (email, phone, addresses) appears.

    4. `## MAKE-FIX-02 verification` — State that the 42-row sample contains zero athlete rows (no `sport_level` value contains "alto"; only categorical values present are `Sedentaria` and `Recreacional 2-3x/sem`). Provide two verification paths:
       - **Non-athlete path (verifiable today):** Use row 5 (`sport_level: Recreacional 2-3x/sem`); confirm Module 2 output has `activity_profile: "non_athlete"`.
       - **Athlete path (synthetic fixture required):** Pass an inline JSON object to Module 2 with `is_athlete: true`; confirm output has `activity_profile: "athlete"`.

    Note the Pitfall D behavior: when `data.is_athlete` is `undefined` (all current sample rows), the condition is not entered and `"non_athlete"` default applies — this is intentional post-fix behavior.

    **Style constraints (from Pattern Map):**
    - H2 only; no H3; ~4 sections (plus one optional `## Known divergences` if you prefer to split out the score_total note from §Tag naming convention).
    - Triple-backtick fences with `bash` or `json` language tags.
    - Inline code uses single backticks for keys, flag names, and paths.
    - Declarative imperative voice ("Run", "Confirm", "Verify"); no first-person plural.
    - No architecture diagrams, no Module 1 vs Module 2 explainer, no deployment playbook, no rollback steps (D-04-04).
    - Target 50–80 lines total.

    **PII compliance (T-PII-01):** Cite rows by index. Categorical enum values (`Perimenopausia`, `Recreacional 2-3x/sem`, `Sedentaria`, `programa-piso-pelvico`, `non_athlete`, `athlete`) are safe inline. The synthetic JSON fixture uses `test@example.com` and placeholder names. No real respondent email, phone, or address appears anywhere.
  </action>
  <verify>
    <automated>cd /Users/silveimar/Documents/silogia-repos/general-python-scripts && F=quizify-csv-to-json-webhook/make-scripts/CONVENTIONS.md && test -f "$F" && grep -q "snake_case" "$F" && grep -q "row 10" "$F" && grep -q "Perimenopausia" "$F" && grep -q "non_athlete" "$F" && grep -q "product-recommendation" "$F" && LINES=$(grep -cv '^#' "$F" || true) && TOTAL=$(wc -l < "$F") && [ "$TOTAL" -ge 40 ] && [ "$TOTAL" -le 120 ] && echo "OK ($TOTAL lines)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f quizify-csv-to-json-webhook/make-scripts/CONVENTIONS.md` exits 0
    - `grep -q "snake_case" quizify-csv-to-json-webhook/make-scripts/CONVENTIONS.md` succeeds (tag canonical-spelling rule present)
    - `grep -q "row 10" quizify-csv-to-json-webhook/make-scripts/CONVENTIONS.md` succeeds (MAKE-FIX-01 verification row cited)
    - `grep -q "Perimenopausia" quizify-csv-to-json-webhook/make-scripts/CONVENTIONS.md` succeeds (categorical enum value cited)
    - `grep -q "non_athlete" quizify-csv-to-json-webhook/make-scripts/CONVENTIONS.md` succeeds (MAKE-FIX-02 verification language present)
    - `grep -q "product-recommendation" quizify-csv-to-json-webhook/make-scripts/CONVENTIONS.md` succeeds (CONTRACT-01 verification language present)
    - File length is between 40 and 120 lines (target was 50–80; ±20 line tolerance for natural variation)
    - No real respondent email or phone number appears (T-PII-01) — checked by `! grep -E '@(gmail|hotmail|yahoo|outlook|icloud)\.' CONVENTIONS.md` (only `test@example.com` is permitted)
  </acceptance_criteria>
  <done>CONVENTIONS.md exists at the target path; all 6 required-content greps succeed; file length within tolerance; no real PII present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation file → human reader | New Markdown file in repo; rendered by GitHub/local viewers; no code execution surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PII-01 (carry-forward) | V8 (Data Protection / PII) | `make-scripts/CONVENTIONS.md` | mitigate | Cite respondent rows by index only (row 10, row 35, row 5); use only categorical enum values inline (`Perimenopausia`, `Recreacional 2-3x/sem`, `non_athlete`, `athlete`); use placeholder PII (`test@example.com`, `Test User`) in the synthetic CONTRACT-01 fixture; allow first names (Karen Retamal, Javielys Mancilla) as row identifiers in a single sentence per D-04-04 / CONTEXT.md, but no email/phone/address. Acceptance criterion `! grep -E '@(gmail|hotmail|...)\.'` enforces no real email leaks. |
| — | V5 (Input Validation) | CONVENTIONS.md | N/A | Documentation file; no input-validation surface |
| — | V2/V3/V4/V6/V7 | CONVENTIONS.md | N/A | Markdown file with no auth, sessions, ACL, crypto, or error-handling surface |

**Honest assessment:** Plan 02 creates a single Markdown verification doc. The only meaningful security concern is T-PII-01 (carry-forward from PROJECT.md). The action text and acceptance criteria explicitly bound what PII may appear (row indices + categorical enums + placeholder fixtures only). No new threat surface introduced.
</threat_model>

<verification>
Run the CONVENTIONS.md gate from VALIDATION.md (combined into the task's `<automated>` block):

```bash
cd /Users/silveimar/Documents/silogia-repos/general-python-scripts
F=quizify-csv-to-json-webhook/make-scripts/CONVENTIONS.md
test -f "$F" && \
  grep -q "snake_case" "$F" && \
  grep -q "row 10" "$F" && \
  grep -q "Perimenopausia" "$F" && \
  grep -q "non_athlete" "$F" && \
  grep -q "product-recommendation" "$F" && \
  echo "CONVENTIONS.md gate: PASS"
```

PII safety check (T-PII-01):
```bash
! grep -E '@(gmail|hotmail|yahoo|outlook|icloud)\.' "$F" && echo "PII gate: PASS"
```

After CONVENTIONS.md lands and is committed, the operator runs the manual Make.com verifications described inside the file (CONTRACT-01 synthetic fixture; MAKE-FIX-01 rows 10/35; MAKE-FIX-02 row 5 + synthetic athlete) — these are out-of-scope for shell-level automation per VALIDATION.md "Manual-Only Verifications" table.
</verification>

<success_criteria>
- CONVENTIONS.md exists at `quizify-csv-to-json-webhook/make-scripts/CONVENTIONS.md`.
- All four mandated topics (D-04-04) are present and identifiable by their section headers.
- File length is 40–120 lines (target 50–80, ±20 tolerance).
- No real respondent email, phone, or address appears (T-PII-01).
- Document references row 10, row 35, and row 5 by index; first names appear at most once as row identifiers per CONTEXT.md.
- Synthetic CONTRACT-01 fixture uses `test@example.com` placeholder.
- Style matches README.md (H2 only, noun-phrase titles, fenced ```bash``` and ```json``` blocks).
</success_criteria>

<output>
After completion, create `.planning/phases/04-make-com-js-contract-fixes/04-02-SUMMARY.md` documenting the new file path, line count, the 6 grep-gate results, and a one-line note that operator manual Make.com verification is now unblocked.
</output>
