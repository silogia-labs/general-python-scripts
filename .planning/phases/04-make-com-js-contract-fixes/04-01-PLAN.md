---
phase: 04-make-com-js-contract-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quizify-csv-to-json-webhook/make-scripts/quizify-mapping.js
  - quizify-csv-to-json-webhook/make-scripts/score-calculations.js
autonomous: true
requirements:
  - CONTRACT-01
  - MAKE-FIX-01
  - MAKE-FIX-02
must_haves:
  truths:
    - "D-05 hyphenated key contract is preserved: `product-recommendation` remains the canonical key Module 1 reads from Python output (D-04-03)."
    - "Snake_case underscores are the canonical tag spelling across both JS files (D-04-02)."
    - "`activity_profile` correctly classifies athletes as `\"athlete\"` and non-athletes as `\"non_athlete\"` (D-04-01)."
  artifacts:
    - path: "quizify-csv-to-json-webhook/make-scripts/quizify-mapping.js"
      provides: "CONTRACT-01 fix: line 103 deletion (product_result dead key removed)"
      contains: "product_recommendation: record[\"product-recommendation\"]"
    - path: "quizify-csv-to-json-webhook/make-scripts/score-calculations.js"
      provides: "MAKE-FIX-01 (peri_menu underscore) + MAKE-FIX-02 (activity_profile inversion fix)"
      contains: "if (data.is_athlete)"
  key_links:
    - from: "Python CLI output (D-05 hyphen keys)"
      to: "quizify-mapping.js line 102"
      via: "record[\"product-recommendation\"]"
      pattern: "record\\[\"product-recommendation\"\\]"
    - from: "quizify-mapping.js emitter (peri_menu)"
      to: "score-calculations.js consumer (line 213)"
      via: "hasTag(tags, \"peri_menu\")"
      pattern: "hasTag\\(tags, \"peri_menu\"\\)"
    - from: "Module 1 is_athlete tag"
      to: "score-calculations.js line 248 condition"
      via: "if (data.is_athlete)"
      pattern: "if \\(data\\.is_athlete\\)"
---

<objective>
Fix three live correctness bugs in the Make.com JS consumer modules by surgical text edits to two files. Bundle all three JS edits into a single plan/commit per ROADMAP "single-pass review" rationale and per CONTEXT.md "Claude's Discretion" note on commit grouping (Option B). After this plan lands: the dead `product_result` key is gone (CONTRACT-01 / D-04-03), the `peri-menu` hyphen consumer outlier is replaced with `peri_menu` underscore (MAKE-FIX-01 / D-04-02), and the inverted `activity_profile` condition is corrected (MAKE-FIX-02 / D-04-01).

Purpose: Restore contract alignment between Python emitter (D-05 locked) and JS consumer; correct two semantic-mis-classification bugs that affect every respondent.
Output: Two modified JS files. Total diff: ~3 lines changed (1 deletion, 1 string change, 1 negation removal).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-make-com-js-contract-fixes/04-CONTEXT.md
@.planning/phases/04-make-com-js-contract-fixes/04-RESEARCH.md
@.planning/phases/04-make-com-js-contract-fixes/04-PATTERNS.md
@.planning/phases/04-make-com-js-contract-fixes/04-VALIDATION.md
@quizify-csv-to-json-webhook/make-scripts/quizify-mapping.js
@quizify-csv-to-json-webhook/make-scripts/score-calculations.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: CONTRACT-01 — Delete dead product_result line in quizify-mapping.js</name>
  <files>quizify-csv-to-json-webhook/make-scripts/quizify-mapping.js</files>
  <read_first>
    - quizify-csv-to-json-webhook/make-scripts/quizify-mapping.js (current state, lines 94-107)
    - .planning/phases/04-make-com-js-contract-fixes/04-CONTEXT.md (D-04-03: pure deletion, no comment stub)
    - .planning/phases/04-make-com-js-contract-fixes/04-RESEARCH.md (CONTRACT-01 section, exact lines 102-103)
  </read_first>
  <action>
    Delete line 103 of `quizify-csv-to-json-webhook/make-scripts/quizify-mapping.js` entirely (per D-04-03).

    The line currently reads exactly:
    `    product_result: record.product_result || null,`

    After deletion: line 102 (`product_recommendation: record["product-recommendation"] || null,`) keeps its trailing comma — this is correct ES5+ JS and matches existing file style (per Pattern Map "Trailing-comma convention"). Line 104 (`title: record.title || null,`) becomes line 103.

    Do NOT replace with a comment. Do NOT rename. Do NOT touch line 102 or any surrounding line. Single-line deletion only.
  </action>
  <verify>
    <automated>cd /Users/silveimar/Documents/silogia-repos/general-python-scripts && [ "$(grep -c 'product_result' quizify-csv-to-json-webhook/make-scripts/quizify-mapping.js)" = "0" ] && [ "$(grep -c 'product_recommendation' quizify-csv-to-json-webhook/make-scripts/quizify-mapping.js)" = "1" ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "product_result" quizify-csv-to-json-webhook/make-scripts/quizify-mapping.js` outputs `0`
    - `grep -c "product_recommendation" quizify-csv-to-json-webhook/make-scripts/quizify-mapping.js` outputs `1`
    - File still parses (visual check of object literal — no orphan commas, all braces balanced)
  </acceptance_criteria>
  <done>Line 103 deleted; `product_result` substring absent from the file; `product_recommendation` key still present exactly once.</done>
</task>

<task type="auto">
  <name>Task 2: MAKE-FIX-01 — Replace "peri-menu" with "peri_menu" in score-calculations.js</name>
  <files>quizify-csv-to-json-webhook/make-scripts/score-calculations.js</files>
  <read_first>
    - quizify-csv-to-json-webhook/make-scripts/score-calculations.js (current state, lines 211-214)
    - .planning/phases/04-make-com-js-contract-fixes/04-CONTEXT.md (D-04-02: ship gate confirmed; consumer-side fix only)
    - .planning/phases/04-make-com-js-contract-fixes/04-RESEARCH.md (MAKE-FIX-01 section + Pitfall A: do NOT touch quizify-mapping.js:167 emitter)
  </read_first>
  <action>
    Change line 213 of `quizify-csv-to-json-webhook/make-scripts/score-calculations.js`.

    Current line reads exactly:
    `const is_peri_meno = hasTag(tags, "peri-menu");`

    Replace with exactly:
    `const is_peri_meno = hasTag(tags, "peri_menu");`

    Single-character change: the hyphen between `peri` and `menu` becomes an underscore. Do NOT modify lines 212 or 214 (`hasTag` calls for `postpartum` and `menstrual`). Do NOT modify `quizify-mapping.js` — the emitter at line 167 already uses the canonical `peri_menu` underscore spelling and is correct (Pitfall A).
  </action>
  <verify>
    <automated>cd /Users/silveimar/Documents/silogia-repos/general-python-scripts && [ "$(grep -c '"peri-menu"' quizify-csv-to-json-webhook/make-scripts/score-calculations.js)" = "0" ] && [ "$(grep -c 'peri_menu' quizify-csv-to-json-webhook/make-scripts/quizify-mapping.js)" = "1" ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"peri-menu"' quizify-csv-to-json-webhook/make-scripts/score-calculations.js` outputs `0`
    - `grep -c "peri_menu" quizify-csv-to-json-webhook/make-scripts/quizify-mapping.js` outputs `1` (emitter untouched)
    - `grep -n 'peri_menu' quizify-csv-to-json-webhook/make-scripts/score-calculations.js` shows the underscore form on line 213
  </acceptance_criteria>
  <done>Line 213 reads `const is_peri_meno = hasTag(tags, "peri_menu");`; no hyphenated `peri-menu` anywhere in either JS file.</done>
</task>

<task type="auto">
  <name>Task 3: MAKE-FIX-02 — Remove negation on activity_profile condition in score-calculations.js</name>
  <files>quizify-csv-to-json-webhook/make-scripts/score-calculations.js</files>
  <read_first>
    - quizify-csv-to-json-webhook/make-scripts/score-calculations.js (current state, lines 245-250)
    - .planning/phases/04-make-com-js-contract-fixes/04-CONTEXT.md (D-04-01: ship gate confirmed; no downstream Airtable consumer)
    - .planning/phases/04-make-com-js-contract-fixes/04-RESEARCH.md (MAKE-FIX-02 section + Pitfall D: undefined-is-falsy behavior is correct)
    - .planning/phases/04-make-com-js-contract-fixes/04-PATTERNS.md (lines 246-250 single-line if-block style; do not add else branch)
  </read_first>
  <action>
    Change line 248 of `quizify-csv-to-json-webhook/make-scripts/score-calculations.js`.

    Current line reads exactly:
    `if (!data.is_athlete) {`

    Replace with exactly:
    `if (data.is_athlete) {`

    Single-character removal: drop the `!` negation. Do NOT modify line 247 (`let activity_profile = "non_athlete";` is the correct default), line 249 (`activity_profile = "athlete";` is the correct branch assignment), or line 250 (closing brace). Do NOT add an `else` branch — the `let` initializer already provides the default per the existing pattern (Pattern Map: "no `else` branch (default is the `let` initializer above)").

    Behavioral note (Pitfall D): `data.is_athlete` is `undefined` for all 42 current sample rows (no athlete fixtures), and `undefined` is falsy → condition is not entered → default `"non_athlete"` applies. This is the intended post-fix behavior.
  </action>
  <verify>
    <automated>cd /Users/silveimar/Documents/silogia-repos/general-python-scripts && [ "$(grep -c '!data.is_athlete' quizify-csv-to-json-webhook/make-scripts/score-calculations.js)" = "0" ] && [ "$(grep -c 'if (data.is_athlete)' quizify-csv-to-json-webhook/make-scripts/score-calculations.js)" = "1" ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '!data.is_athlete' quizify-csv-to-json-webhook/make-scripts/score-calculations.js` outputs `0`
    - `grep -c 'if (data.is_athlete)' quizify-csv-to-json-webhook/make-scripts/score-calculations.js` outputs `1`
    - Line 247 still reads `let activity_profile = "non_athlete";` (default unchanged)
    - Line 249 still reads `    activity_profile = "athlete";` (branch assignment unchanged)
  </acceptance_criteria>
  <done>Line 248 reads `if (data.is_athlete) {`; no `!data.is_athlete` substring remains in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python CLI output → Make.com Module 1 input | JSON envelope received via Make.com `input` object; trusted upstream (our own CLI emit) |
| Module 1 output → Module 2 input | In-Make.com module-to-module data flow; no external untrusted input introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| — | V2/V3/V4/V6 | both JS files | N/A | Stateless IIFE modules — no auth, sessions, ACL, or crypto operations |
| — | V5 (Input Validation) | both JS files | accept | JS reads from Make.com-controlled `input` object passed by upstream module; CONTRACT-01 actually tightens the contract by removing a dead key. No new input-validation surface introduced; no new validation required for these edits. |
| — | V7/V8 (Error/PII) | both JS files | N/A | No new log surfaces; no PII handling changes; no error-message changes. T-PII-01 not implicated by code-only edits. |

**Honest assessment:** Phase 4 Plan 01 is three text-level edits to existing IIFE consumer modules. There is no new threat surface. The CONTRACT-01 deletion narrows the surface (one fewer key emitted); MAKE-FIX-01 and MAKE-FIX-02 correct misclassifications without touching any input-validation or PII path.
</threat_model>

<verification>
After all three tasks land, run the full pre-Wave-2 grep gate suite from RESEARCH.md §"Validation Architecture":

```bash
cd /Users/silveimar/Documents/silogia-repos/general-python-scripts
grep -c "product_result" quizify-csv-to-json-webhook/make-scripts/quizify-mapping.js               # expect 0
grep -c "product_recommendation" quizify-csv-to-json-webhook/make-scripts/quizify-mapping.js      # expect 1
grep -c '"peri-menu"' quizify-csv-to-json-webhook/make-scripts/score-calculations.js              # expect 0
grep -c "peri_menu" quizify-csv-to-json-webhook/make-scripts/quizify-mapping.js                   # expect 1
grep -c '!data.is_athlete' quizify-csv-to-json-webhook/make-scripts/score-calculations.js         # expect 0
grep -c 'if (data.is_athlete)' quizify-csv-to-json-webhook/make-scripts/score-calculations.js     # expect 1
```

All six lines must match expected outputs. Manual Make.com runtime verification is documented in Plan 02 (CONVENTIONS.md) and performed by the operator post-deploy.
</verification>

<success_criteria>
- All 6 grep gates above output their expected counts.
- Diff against pre-phase HEAD shows exactly: 1 line deleted from `quizify-mapping.js`; 2 single-character changes in `score-calculations.js` (line 213 hyphen→underscore; line 248 `!` removal).
- No other files modified.
- Both JS files remain valid JavaScript (no orphan commas, no unclosed braces).
</success_criteria>

<output>
After completion, create `.planning/phases/04-make-com-js-contract-fixes/04-01-SUMMARY.md` documenting the three edits, the grep-gate evidence, and a pointer that Make.com runtime verification is the operator's responsibility per CONVENTIONS.md (Plan 02).
</output>
