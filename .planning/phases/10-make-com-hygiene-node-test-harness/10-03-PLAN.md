---
phase: 10-make-com-hygiene-node-test-harness
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - .github/workflows/ci.yml
  - quizify-csv-to-json-webhook/README.md
autonomous: true
requirements:
  - MAKE-TEST-01
  - MAKE-TEST-03
tags:
  - ci
  - docs
  - make-scripts

must_haves:
  truths:
    - "GitHub Actions runs `node --test` on every PR via a `make-scripts-test` job"
    - "GitHub Actions runs the existing pytest suite (including new test_make_scripts_no_deps.py + test_make_scripts_no_pii.py)"
    - "README documents `node --test quizify-csv-to-json-webhook/make-scripts/` invocation as a `###` subsection under the existing `## Development` section"
    - "D-11 README ten-section drift test (`tests/test_readme_help_alignment.py`, 2/2) remains green"
    - "`grep -c '^## ' quizify-csv-to-json-webhook/README.md` still returns exactly 10"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "Fresh CI workflow with two parallel jobs: `pytest` and `make-scripts-test` (D-10-17). Created fresh because `.github/workflows/` was empty in working tree (RESEARCH OQ#1 resolution)."
      contains: 'make-scripts-test'
    - path: quizify-csv-to-json-webhook/README.md
      provides: "`### Make.com module tests` subsection under existing `## Development` (D-10-20 invocation; D-11 ten-section lock preserved)"
      contains: 'node --test'
  key_links:
    - from: ".github/workflows/ci.yml make-scripts-test job"
      to: "quizify-csv-to-json-webhook/make-scripts/"
      via: "working-directory + `node --test`"
      pattern: 'working-directory:.*make-scripts'
    - from: "README.md ## Development"
      to: "make-scripts/ test invocation"
      via: "### Make.com module tests subsection"
      pattern: '### Make.com'
---

<objective>
Plan 10-03 — CI wiring + README documentation (parallelizable with Plan 10-02; depends only on Plan 10-01's artifacts).

Resolve PATTERNS.md open item #1 (GH Actions workflow path): `find .github -type f` returned empty in the working tree at planning time, and `git ls-files | grep '\.github'` shows only `.cursor/rules/...`. There is no existing CI workflow file. Per RESEARCH OQ#1 resolution, this plan **creates** a fresh `.github/workflows/ci.yml` with two parallel jobs: an existing-style `pytest` job and the new `make-scripts-test` job (D-10-17).

Add a `### Make.com module tests` subsection under the existing `## Development` heading in `quizify-csv-to-json-webhook/README.md` (Pitfall 5 — never add a new top-level `##` heading; D-11 ten-section lock preserved).

Output: 1 new file (`.github/workflows/ci.yml`), 1 edited file (README.md). No source-logic changes; no test changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-make-com-hygiene-node-test-harness/10-CONTEXT.md
@.planning/phases/10-make-com-hygiene-node-test-harness/10-RESEARCH.md
@.planning/phases/10-make-com-hygiene-node-test-harness/10-PATTERNS.md
@quizify-csv-to-json-webhook/README.md
@quizify-csv-to-json-webhook/tests/test_readme_help_alignment.py

<interfaces>
<!-- Locked code skeletons. PASTE VERBATIM. -->

`.github/workflows/ci.yml` (D-10-17 + Pitfall 4 — NO `cache: 'npm'`; resolves RESEARCH OQ#1 by creating fresh workflow file):
```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  pytest:
    name: pytest (quizify-csv-to-json-webhook)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install package + test deps
        working-directory: quizify-csv-to-json-webhook
        run: pip install -e '.[test]' || pip install -e .
      - name: Run pytest
        working-directory: quizify-csv-to-json-webhook
        run: pytest -q

  make-scripts-test:
    name: make-scripts node:test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          # NO `cache: 'npm'` — empty deps, no lockfile (RESEARCH Pitfall 4).
      - name: Run node:test
        working-directory: quizify-csv-to-json-webhook/make-scripts
        run: node --test
```

README subsection (D-10-20 + Pitfall 5 — `###` under existing `## Development`; never add a new `##`):
```markdown
### Make.com module tests

The `make-scripts/` directory ships two co-owned Make.com IIFE modules
(`quizify-mapping.js`, `score-calculations.js`) plus a zero-dependency
`node:test` regression suite under `make-scripts/tests/`.

Run from repo root (Node 20+ required):

```sh
node --test quizify-csv-to-json-webhook/make-scripts/
```

Or, equivalent shorthand for operators inside `make-scripts/`:

```sh
cd quizify-csv-to-json-webhook/make-scripts && npm test
```

No `npm install` is ever run — the package ships with empty `dependencies`
and `devDependencies`, enforced by `tests/test_make_scripts_no_deps.py` in
CI. Test fixtures under `make-scripts/tests/fixtures/` are synthetic-only
(T-PII-01 carry-forward), enforced by `tests/test_make_scripts_no_pii.py`.

After merging changes to `quizify-mapping.js` or `score-calculations.js`,
operators must paste the updated module body into the live Make.com Code
modules and re-run the inline-JSON verification fixtures in
`make-scripts/CONVENTIONS.md` (CONTRACT-01 / MAKE-FIX-01 / MAKE-FIX-02).
The `node --test` suite catches regressions before paste; CONVENTIONS.md
fixtures catch operator drift after paste.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 10-03-01: Create fresh .github/workflows/ci.yml with pytest + make-scripts-test jobs</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - Run `git ls-files | grep -E '\.github|workflow'` and `find . -path ./node_modules -prune -o -type d -name .github -print` to CONFIRM no existing workflow file (planning verified empty; re-confirm at execute time per RESEARCH OQ#1).
    - .planning/phases/10-make-com-hygiene-node-test-harness/10-RESEARCH.md §"Pitfall 4" (no `cache: 'npm'` without lockfile) and §"Code Examples" Example 3.
    - quizify-csv-to-json-webhook/pyproject.toml (confirm test extras layout, if any).
  </read_first>
  <action>
    1. **Re-verify** at execute time that no `.github/workflows/*.yml` file exists. If one DOES exist (contradicting RESEARCH OQ#1), append the `make-scripts-test` job to that file instead of creating a new one — D-10-17 says "added to the **existing** CI workflow"; only create fresh if genuinely absent.
    2. If absent (expected), create `.github/workflows/ci.yml` by pasting verbatim from `<interfaces>` above.
    3. The pytest job uses `pip install -e '.[test]' || pip install -e .` — falls back to non-extras install if pyproject has no `[test]` extras section. Adjust the install command if pyproject.toml uses a different idiom (e.g., `requirements-test.txt`); preserve D-13 (no new runtime deps; only test deps).
    4. The `make-scripts-test` job MUST NOT include `cache: 'npm'` (Pitfall 4 — empty deps means no lockfile means cache fails). Use Node 20 LTS per D-10-17.
    5. Both jobs run on `ubuntu-latest`; both checkout via `actions/checkout@v4`. Jobs run in parallel (no `needs:` between them).

    Do NOT add lint/format/coverage jobs — D-13 + D-10-15 keep the harness stdlib-only. Do NOT add a top-level `repo/package.json` (REQUIREMENTS.md "Out of Scope" forbids it).
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q 'make-scripts-test' .github/workflows/ci.yml && grep -q 'node-version: "20"' .github/workflows/ci.yml && ! grep -q "cache:" .github/workflows/ci.yml</automated>
    Optional: `npx --yes @action-validator/cli@latest .github/workflows/ci.yml` (skip if action-validator unavailable; YAML syntax check via `python -c 'import yaml; yaml.safe_load(open(".github/workflows/ci.yml"))'` is acceptable fallback).
  </verify>
  <done>
    `.github/workflows/ci.yml` exists with two jobs (`pytest`, `make-scripts-test`); make-scripts job runs `node --test` in `quizify-csv-to-json-webhook/make-scripts` working-directory with Node 20 and NO npm cache. YAML parses cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 10-03-02: Add `### Make.com module tests` subsection under `## Development` in README.md</name>
  <files>quizify-csv-to-json-webhook/README.md</files>
  <read_first>
    - quizify-csv-to-json-webhook/README.md (full — locate `## Development` section at the end; verify exactly 10 top-level `##` headings before edit)
    - quizify-csv-to-json-webhook/tests/test_readme_help_alignment.py (full — confirm REQUIRED_SECTIONS list and that no count assertion exists; D-11 lock semantics)
    - .planning/phases/10-make-com-hygiene-node-test-harness/10-RESEARCH.md §"Pitfall 5" (D-11 ten-section drift)
    - .planning/phases/10-make-com-hygiene-node-test-harness/10-PATTERNS.md §"`quizify-csv-to-json-webhook/README.md`"
  </read_first>
  <action>
    1. Pre-edit verify: `grep -c '^## ' quizify-csv-to-json-webhook/README.md` returns exactly 10 (Purpose, Quickstart, CLI reference, Configuration, Column assumptions, Output shape, Limitations, Privacy notes, Exit codes, Development).
    2. Append the `### Make.com module tests` subsection (verbatim from `<interfaces>` above) at the END of the existing `## Development` section. Do NOT add any new `## ...` top-level heading. Do NOT modify or reorder any existing `## ...` heading. The subsection uses `###` (h3) only.
    3. Post-edit verify: section count still 10. Drift test still passes.
  </action>
  <verify>
    <automated>test "$(grep -c '^## ' quizify-csv-to-json-webhook/README.md)" -eq 10 && grep -q '^### Make.com module tests' quizify-csv-to-json-webhook/README.md && cd quizify-csv-to-json-webhook && pytest tests/test_readme_help_alignment.py -q</automated>
  </verify>
  <done>
    README has new `### Make.com module tests` subsection under `## Development`; section count remains 10; D-11 drift test still 2/2 green.
  </done>
</task>

<task type="auto">
  <name>Task 10-03-03: Final full-suite green check + grep gates</name>
  <files></files>
  <read_first>
    - .planning/phases/10-make-com-hygiene-node-test-harness/10-VALIDATION.md §"Per-Task Verification Map" rows 10-03-05, 10-03-06.
  </read_first>
  <action>
    Run the full validation suite to confirm no regressions:

    1. `cd quizify-csv-to-json-webhook && pytest -q` — expect all green (158 baseline + 4 from test_make_scripts_no_deps + 1 from test_make_scripts_no_pii = 163; or whatever the post-Plan-10-01 baseline is).
    2. `cd quizify-csv-to-json-webhook/make-scripts && node --test` — expect exit 0 with all six test files green (Plan 10-02 already flipped the cosmetic tests).
    3. v1.1 grep gates (carry-forward regression): `cd quizify-csv-to-json-webhook && pytest tests/test_security_grep_gates.py tests/test_grep_gates.py 2>/dev/null -q || pytest tests/test_security_grep_gates.py -q`
    4. README drift: `cd quizify-csv-to-json-webhook && pytest tests/test_readme_help_alignment.py -q`.
    5. CI workflow YAML parses: `python -c 'import yaml; yaml.safe_load(open(".github/workflows/ci.yml"))'`.

    No code changes in this task — verification-only. If any check fails, file the failure as a Plan 10-04 gap-closure candidate (do NOT fix in this plan).
  </action>
  <verify>
    <automated>cd quizify-csv-to-json-webhook && pytest -q && cd make-scripts && node --test && cd .. && pytest tests/test_readme_help_alignment.py -q && python -c 'import yaml; yaml.safe_load(open("../.github/workflows/ci.yml"))'</automated>
  </verify>
  <done>
    Both runtimes green; README drift test green; CI YAML parses. Phase 10 ready for `/gsd-verify-work 10`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner ↔ npm registry | With empty `dependencies`/`devDependencies` and no lockfile, `npm install` is never invoked — supply-chain surface area is zero. |
| README documentation ↔ D-11 ten-section lock | Adding a new `##` heading would silently break the conceptual lock; subsection-only addition keeps the contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Tampering | Future PR adds `cache: 'npm'` to setup-node, breaking CI on missing lockfile | accept | Pitfall 4 documented in RESEARCH; CI failure is loud and fast — no silent regression risk. |
| T-10-08 | Tampering | Future README addition introduces 11th `##` section, drifting from D-11 lock | mitigate | `tests/test_readme_help_alignment.py` REQUIRED_SECTIONS asserts presence; subsection rule documented in PATTERNS.md. (Future hardening: add a count assertion — deferred.) |
</threat_model>

<verification>
1. `.github/workflows/ci.yml` exists with two parallel jobs (`pytest`, `make-scripts-test`).
2. `make-scripts-test` uses Node 20, NO `cache:` key, runs `node --test` in `make-scripts/` working directory.
3. README has new `### Make.com module tests` subsection under `## Development`; section count still 10; D-11 drift test green.
4. Full suite: pytest green + `node --test` green + v1.1 grep gates green.
5. CI YAML parses without errors.
</verification>

<success_criteria>
- MAKE-TEST-01 (CI gate): `make-scripts-test` job wired in fresh `.github/workflows/ci.yml`.
- MAKE-TEST-03 (operator-facing docs): README subsection documents `node --test` invocation; D-11 lock preserved.
- All 5 ROADMAP Phase 10 success criteria addressable: criterion #1 (cosmetic-01/02 tests + fixes) covered by Plan 10-01 + Plan 10-02; criterion #2 (CONTRACT-01 / MAKE-FIX-01 / MAKE-FIX-02 / MAKE-COSMETIC-01/02 with inline citations) covered by Plan 10-01; criterion #3 (mapRecord export + dual-export footer + use-strict + globalThis snapshot) covered by Plan 10-01; criterion #4 (empty-deps gate + norecursedirs + .gitignore) covered by Plan 10-01; criterion #5 (synthetic fixtures + README drift green) covered by Plan 10-01 + this plan.
- All 5 phase requirement IDs (MAKE-COSMETIC-01/02, MAKE-TEST-01/02/03) appear in at least one of the three plans (verified in source-audit).
</success_criteria>

<output>
After completion, create `.planning/phases/10-make-com-hygiene-node-test-harness/10-03-SUMMARY.md`.
</output>
