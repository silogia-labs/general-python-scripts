---
phase: 10-make-com-hygiene-node-test-harness
plan: 02
type: tdd
wave: 2
depends_on: ["10-01"]
files_modified:
  - quizify-csv-to-json-webhook/make-scripts/score-calculations.js
autonomous: true
gate: GREEN
requirements:
  - MAKE-COSMETIC-01
  - MAKE-COSMETIC-02
tags:
  - make-scripts
  - tdd-green
  - cosmetic-fix

must_haves:
  truths:
    - "`out.context_profile === 'Remoto'` (not 'Reomoto') for the work-remoto fixture"
    - "Literal string `Reomoto` does not appear anywhere in score-calculations.js source or in any fixture's mapRecord output"
    - "Literal string `profile_base` does not appear anywhere in score-calculations.js source or in any fixture's mapRecord output"
    - "All six node:test files green (`node --test` exits 0)"
    - "No other behavior changes — every profile branch still produces its original value (red_flags, high_complexity, moderate_complexity, low_complexity)"
  artifacts:
    - path: quizify-csv-to-json-webhook/make-scripts/score-calculations.js
      provides: "MAKE-COSMETIC-01 fix at line 157 (Reomoto → Remoto) + MAKE-COSMETIC-02 fix at line 217 (`let profile = \"profile_base\";` → `let profile;`)"
      contains: 'return "Remoto"'
  key_links:
    - from: "make-scripts/tests/cosmetic-01.test.js"
      to: "score-calculations.js calculateContextProfile remoto branch"
      via: "out.context_profile === 'Remoto'"
      pattern: 'context_profile.*Remoto'
    - from: "make-scripts/tests/cosmetic-02.test.js"
      to: "score-calculations.js profile assignment block"
      via: "fixture sweep — out.profile never 'profile_base'"
      pattern: 'profile_base'
---

<objective>
Plan 10-02 — GREEN cosmetic fixes (TDD gate: GREEN).

Apply the two locked source fixes that flip Plan 10-01's RED tests green:

1. `score-calculations.js:157`: `return "Reomoto";` → `return "Remoto";` (MAKE-COSMETIC-01).
2. `score-calculations.js:217`: `let profile = "profile_base";` → `let profile;` (MAKE-COSMETIC-02).

The diff is purely cosmetic — every code path in lines 220–233 unconditionally reassigns `profile`, so removing the dead initializer changes no behavior (verified A6 in RESEARCH). After this plan, `node --test` exits 0 with all six files green; the failing-then-passing trail (Plan 10-01 RED commit → Plan 10-02 GREEN commit) is visible in `git log` per D-10-10.

Output: one file edit (score-calculations.js); two grep-verifiable assertions; six green node:test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-make-com-hygiene-node-test-harness/10-CONTEXT.md
@.planning/phases/10-make-com-hygiene-node-test-harness/10-RESEARCH.md
@.planning/phases/10-make-com-hygiene-node-test-harness/10-PATTERNS.md
@.planning/phases/10-make-com-hygiene-node-test-harness/10-01-PLAN.md
@quizify-csv-to-json-webhook/make-scripts/score-calculations.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 10-02-01: Apply MAKE-COSMETIC-01 fix at score-calculations.js:157 (Reomoto → Remoto)</name>
  <files>quizify-csv-to-json-webhook/make-scripts/score-calculations.js</files>
  <read_first>
    - quizify-csv-to-json-webhook/make-scripts/score-calculations.js (full — locate `if (work.includes("remoto")) return "Reomoto";` inside `calculateContextProfile`)
    - quizify-csv-to-json-webhook/make-scripts/tests/cosmetic-01.test.js (Plan 10-01 — failing assertion this fix flips green)
    - .planning/phases/10-make-com-hygiene-node-test-harness/10-RESEARCH.md §"Pitfall 6" (sequencing — this is the GREEN commit)
  </read_first>
  <behavior>
    - Source no longer contains the literal string `"Reomoto"`.
    - For the `work-remoto.json` fixture, `mapRecord(fixture).context_profile === "Remoto"`.
    - `cosmetic-01.test.js` flips from RED to GREEN.
    - All other branches of `calculateContextProfile` (Minería, Atleta, Senior, Doble jornada, Hogar, Oficinista, Nocturno, Otro) are unchanged.
  </behavior>
  <action>
    In `quizify-csv-to-json-webhook/make-scripts/score-calculations.js`, locate the line:

    ```js
    if (work.includes("remoto")) return "Reomoto";
    ```

    Replace with:

    ```js
    if (work.includes("remoto")) return "Remoto";  // FIX MAKE-COSMETIC-01 (was "Reomoto")
    ```

    Single-character substitution within the string literal (drop the second `o` after `Re`). Do NOT touch any other line. Do NOT modify the calling helper, the assignment to `out.context_profile`, or the `tags.push(context_profile)` site — the typo flows through to `out.context_profile` per RESEARCH A5; fixing the source is sufficient.
  </action>
  <verify>
    <automated>! grep -n '"Reomoto"' quizify-csv-to-json-webhook/make-scripts/score-calculations.js && cd quizify-csv-to-json-webhook/make-scripts && node --test tests/cosmetic-01.test.js</automated>
  </verify>
  <done>
    Source contains `"Remoto"` and not `"Reomoto"`. `cosmetic-01.test.js` exits 0 (both positive assertion and negative-regression sweep pass).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 10-02-02: Apply MAKE-COSMETIC-02 fix at score-calculations.js:217 (remove dead profile_base initializer)</name>
  <files>quizify-csv-to-json-webhook/make-scripts/score-calculations.js</files>
  <read_first>
    - quizify-csv-to-json-webhook/make-scripts/score-calculations.js (full — locate `let profile = "profile_base";` followed by the `if (data.has_red_flags) { profile = "red_flags"; } else { ... }` block that unconditionally reassigns)
    - quizify-csv-to-json-webhook/make-scripts/tests/cosmetic-02.test.js (Plan 10-01 — failing sweep this fix flips green)
    - .planning/phases/10-make-com-hygiene-node-test-harness/10-RESEARCH.md A6 (branch-coverage rationale: `classifyTotalScore` always returns severo/moderado/leve; every code path reassigns)
  </read_first>
  <behavior>
    - Source no longer contains the literal string `"profile_base"`.
    - For every fixture (red_flags, severo, moderado, leve branches), `mapRecord(fixture).profile` is one of `"red_flags"`, `"high_complexity"`, `"moderate_complexity"`, `"low_complexity"` — NEVER `"profile_base"`.
    - `cosmetic-02.test.js` flips from RED to GREEN; both the `out.profile !== "profile_base"` assertion and the `JSON.stringify(out).includes("profile_base")` belt-and-suspenders check pass.
    - All four profile values across branches are unchanged from Plan 10-01.
  </behavior>
  <action>
    In `quizify-csv-to-json-webhook/make-scripts/score-calculations.js`, locate the line:

    ```js
    let profile = "profile_base";
    ```

    Replace with:

    ```js
    let profile;  // FIX MAKE-COSMETIC-02 — every branch in the immediately following if/else chain reassigns
    ```

    Do NOT touch the immediately-following `if (data.has_red_flags) { profile = "red_flags"; } else { if (score_level === "severo") { ... } else if (score_level === "moderado") { ... } else { profile = "low_complexity"; ... } }` block — those four reassignments cover every path and are the entire reason this initializer is safely removable per RESEARCH A6.

    Do NOT touch `email_template_id` or any other line.
  </action>
  <verify>
    <automated>! grep -n '"profile_base"' quizify-csv-to-json-webhook/make-scripts/score-calculations.js && cd quizify-csv-to-json-webhook/make-scripts && node --test 2>&1 | tee /tmp/p10-02-final.txt; grep -qE '^# fail 0$' /tmp/p10-02-final.txt</automated>
    Full-suite green check: `cd quizify-csv-to-json-webhook && pytest -q && cd make-scripts && node --test` — both runtimes exit 0.
  </verify>
  <done>
    Source no longer contains `"profile_base"`. Full `node --test` run exits 0 with all six files green. Full pytest run still 158+5 = 163 green. RED→GREEN trail visible in git log: Plan 10-01 commit shows tests added with typo PRESERVED; Plan 10-02 commit shows the two-line cosmetic diff.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code ↔ Make.com live deployment | Edits to JS modules must be paste-compatible (D-10-05). Cosmetic-only diff in this plan trivially satisfies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering (data quality) | `Reomoto` typo silently misclassifying users in production | mitigate | RED-before-GREEN trail (Plan 10-01 RED → Plan 10-02 GREEN) makes the bug-then-fix visible in git log; `cosmetic-01.test.js` blocks regression. |
| T-10-06 | Tampering | Future refactor reintroduces dead init or shadows `profile` variable | mitigate | `cosmetic-02.test.js` sweeps all fixtures + JSON.stringify grep — any literal `"profile_base"` reappearance fails the test. |
</threat_model>

<verification>
1. `grep -c '"Reomoto"\|"profile_base"' quizify-csv-to-json-webhook/make-scripts/score-calculations.js` returns 0.
2. `cd quizify-csv-to-json-webhook/make-scripts && node --test` exits 0; TAP shows `# fail 0` (or no fail line).
3. `cd quizify-csv-to-json-webhook && pytest -q` still 158+5 = 163 green.
4. `git log --oneline -3` shows: (Plan 10-02) cosmetic fixes commit; (Plan 10-01) RED scaffolding commit; previous Phase 9 close. RED-before-GREEN trail per D-10-10 visible.
</verification>

<success_criteria>
- MAKE-COSMETIC-01 fix landed: `Reomoto` → `Remoto` at score-calculations.js:157.
- MAKE-COSMETIC-02 fix landed: dead `profile = "profile_base"` initializer removed at :217.
- All six `node:test` files green; full Python pytest suite green.
- Diff is purely cosmetic: no helper/branch logic touched; no fixture changes; no test changes.
- RED→GREEN commit trail visible per D-10-10.
</success_criteria>

<output>
After completion, create `.planning/phases/10-make-com-hygiene-node-test-harness/10-02-SUMMARY.md`.
</output>
