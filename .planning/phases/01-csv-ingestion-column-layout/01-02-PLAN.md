---
wave: 1
plan_id: "01-02"
title: "Smoke-test against sample CSV row count"
requirements:
  - CONV-01
depends_on:
  - "01-01"
files_modified:
  - quizify-csv-to-json-webhook/tests/test_layout.py
autonomous: true
---

# Plan 01-02: Smoke-test CLI against fixture

## Objective

Lock **roadmap smoke criterion**: parsed **data row count** matches the sample export (**42** data rows), via automated test calling the CLI or shared row-count helper — **no manual-only gate**.

## must_haves

- `pytest quizify-csv-to-json-webhook/tests/test_layout.py -q` includes an assertion that **data row count == 42** for `quizify-submissions.csv`.
- stderr from `--dry-run` continues to include **`Rows (data): 42`** when run on the committed fixture path.

<threat_model>
**ASVS L1**

| Threat | Mitigation |
|--------|------------|
| Subprocess leaks env / shell injection | Tests invoke `python -m` or `sys.executable` with argument list; fixture path is repo-relative constant string. |

**Block on:** none.
</threat_model>

## Tasks

<task id="01-02-T1" type="tdd">
<objective>Automated smoke: row count 42 and subprocess `--dry-run` stderr contains Rows line.</objective>
<read_first>
- `quizify-csv-to-json-webhook/quizify_csv_ingest.py`
- `quizify-csv-to-json-webhook/tests/test_layout.py`
- `.planning/phases/01-csv-ingestion-column-layout/01-RESEARCH.md` (row count verification)
</read_first>
<action>
Extend `quizify-csv-to-json-webhook/tests/test_layout.py`:

1. Add `test_data_row_count_matches_fixture`: open `quizify-submissions.csv` with production settings; use `csv.reader`; consume all rows after header; assert `len(data_rows) == 42`.

2. Add `test_dry_run_stderr_row_count` using `subprocess.run([sys.executable, str(REPO_ROOT / "quizify-csv-to-json-webhook/quizify_csv_ingest.py"), "--dry-run", str(FIXTURE)], capture_output=True, text=True, timeout=30)`  
   - Assert `returncode == 0`
   - Assert `"Rows (data): 42" in result.stderr`
   - Assert `@` not in result.stderr **OR** if `@` appears, it must only be from header names — simpler rule: assert **`Rows (data): 42` in result.stderr** and **`Questions (dynamic): 20` in result.stderr** (no email substring requirement if headers lack `@`; optional tighten: assert stderr does not contain a known fixture email substring like `marygodoy11` — include if easy).

Use `pathlib` to resolve fixture relative to test file location (`Path(__file__).resolve().parents[1] / "docs" / "quizify-submissions.csv"`).

</action>
<acceptance_criteria>
- `grep -q "Rows (data): 42" quizify-csv-to-json-webhook/tests/test_layout.py`
- `grep -q "42" quizify-csv-to-json-webhook/tests/test_layout.py`
- `pytest quizify-csv-to-json-webhook/tests/test_layout.py -q` exits 0
</acceptance_criteria>
</task>

## Verification criteria

- Roadmap plan **01-02** satisfied: smoke-test against sample CSV row count is **automated** in pytest.
