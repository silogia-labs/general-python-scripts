---
phase: 06-json-schema-validation
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - quizify-csv-to-json-webhook/pyproject.toml
autonomous: true
requirements: [VALI-05]
tags: [python, packaging, pyproject, flit-core, optional-extra]

must_haves:
  truths:
    - "`quizify-csv-to-json-webhook/pyproject.toml` exists with PEP 621 metadata."
    - "`pip install '.[validate]'` (run from `quizify-csv-to-json-webhook/`) resolves `fastjsonschema>=2.21.2`."
    - "`pip install .` (no extra) resolves NOTHING beyond stdlib — D-13 stdlib-only-at-runtime preserved."
    - "Build backend is `flit_core >=3.2,<4`; the single-module `quizify_csv_ingest.py` is declared via `[tool.flit.module]`."
  artifacts:
    - path: "quizify-csv-to-json-webhook/pyproject.toml"
      provides: "Minimal PEP 621 packaging metadata + `[validate]` optional extra."
      contains: "fastjsonschema>=2.21.2"
  key_links:
    - from: "pyproject.toml [project.optional-dependencies] validate"
      to: "fastjsonschema PyPI package"
      via: "pip install '.[validate]' resolution"
      pattern: 'validate\\s*=\\s*\\["fastjsonschema>=2.21.2"\\]'
---

<objective>
Land the minimal flit_core PEP 621 packaging metadata that makes `pip install '.[validate]'` resolvable. Independent of the schema artifact (Plan 01) and the helper wiring (Plan 03) — runs in the same wave as Plan 01.

Purpose: Without this file, the README install line in Plan 04 is a documentation lie. With it, operators can `cd quizify-csv-to-json-webhook && pip install '.[validate]'` and get fastjsonschema; running `pip install .` without the extra still leaves runtime stdlib-only (D-13).
Output: `quizify-csv-to-json-webhook/pyproject.toml` (new — one file, ~25 lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-json-schema-validation/06-CONTEXT.md
@.planning/phases/06-json-schema-validation/06-RESEARCH.md
@.planning/phases/06-json-schema-validation/06-PATTERNS.md
@confluence-to-markdown/pyproject.toml

<interfaces>
<!-- Locked PEP 621 keys from CONTEXT D-06-01..D-06-05. -->

[project]
  name             = "quizify-csv-to-json-webhook"   # D-06-05; hyphenated
  version          = "1.1.0"                          # D-06-05; v1.1 milestone
  requires-python  = ">=3.9"                          # D-06-03; PEP 585 generics
  readme           = "README.md"
  description      = "Quizify CSV → webhook JSON CLI" (Claude's discretion — terse string)
  license          = { text = "MIT" }                 (Claude's discretion — sibling style)

[project.optional-dependencies]
  validate         = ["fastjsonschema>=2.21.2"]       # D-06-01; LOCKED extra name (Pitfall 24)

[build-system]
  requires         = ["flit_core >=3.2,<4"]           # D-06-02
  build-backend    = "flit_core.buildapi"             # D-06-02

[tool.flit.module]
  name             = "quizify_csv_ingest"             # D-06-04; underscored module

# NO [project.dependencies] — D-13 + Pitfall 20.
# NO [tool.ruff] / [tool.mypy] — sibling has them; not introduced this phase.
</interfaces>
</context>

<tasks>

<task type="execute">
  <name>Task 1: Author quizify-csv-to-json-webhook/pyproject.toml (D-06-01..D-06-05)</name>
  <files>quizify-csv-to-json-webhook/pyproject.toml</files>
  <read_first>
    - .planning/phases/06-json-schema-validation/06-CONTEXT.md (D-06-01..D-06-05)
    - .planning/phases/06-json-schema-validation/06-RESEARCH.md ("Pattern 3: flit_core single-module project"; Pitfall 20; Pitfall 24)
    - .planning/phases/06-json-schema-validation/06-PATTERNS.md (section "pyproject.toml")
    - confluence-to-markdown/pyproject.toml (sibling reference for flit_core block — DIVERGE on dependencies and tool blocks)
  </read_first>
  <action>
Create `quizify-csv-to-json-webhook/pyproject.toml` with EXACTLY the content below. Do NOT add `[project.dependencies]` (Pitfall 20 — D-13 silently broken if present). Do NOT pluralize or re-case the extra name (Pitfall 24 — `validate`, lowercase, singular).

```toml
[project]
name = "quizify-csv-to-json-webhook"
version = "1.1.0"
description = "Quizify CSV → webhook JSON CLI"
requires-python = ">=3.9"
readme = "README.md"
license = { text = "MIT" }

[project.optional-dependencies]
validate = ["fastjsonschema>=2.21.2"]

[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "quizify_csv_ingest"
```

DIVERGENCES from sibling `confluence-to-markdown/pyproject.toml` (intentional, locked):
- NAME: `quizify-csv-to-json-webhook` (D-06-05) — hyphenated, matches directory.
- VERSION: `1.1.0` (D-06-05) — v1.1 milestone deliverable.
- REQUIRES-PYTHON: `>=3.9` (D-06-03) — PEP 585 generics in `quizify_csv_ingest.py`. Sibling uses `>=3.8` because it doesn't.
- NO `[project.dependencies]` — D-13 + Pitfall 20 (sibling has 4 runtime deps; we have zero).
- ADD `[project.optional-dependencies] validate = ["fastjsonschema>=2.21.2"]` — D-06-01 (sibling has none).
- ADD `[tool.flit.module] name = "quizify_csv_ingest"` — D-06-04 (sibling does NOT have this; flit's default hyphen↔underscore rule produces `confluence_to_md` from `confluence-to-md` and matches its single .py file; our project name `quizify-csv-to-json-webhook` would default-substitute to `quizify_csv_to_json_webhook` which does NOT match our actual module file `quizify_csv_ingest.py` — explicit override required).
- DROP `[tool.ruff]` and `[tool.mypy]` blocks — sibling has them; not introduced this phase. The repo has no ruff/mypy CI gate to satisfy.
- DROP `authors = [...]` — sibling has placeholder authors; we omit (it's optional in PEP 621).

File MUST end with a single trailing newline.

Commit message: `feat(06-02): add pyproject.toml with [validate] optional extra (VALI-05 packaging)`
  </action>
  <verify>
    <automated>cd quizify-csv-to-json-webhook && python -c "import tomllib; d=tomllib.loads(open('pyproject.toml','rb').read().decode('utf-8')); assert d['project']['name']=='quizify-csv-to-json-webhook'; assert d['project']['version']=='1.1.0'; assert d['project']['requires-python']=='>=3.9'; assert d['project']['optional-dependencies']['validate']==['fastjsonschema>=2.21.2']; assert d['build-system']['requires']==['flit_core >=3.2,<4']; assert d['build-system']['build-backend']=='flit_core.buildapi'; assert d['tool']['flit']['module']['name']=='quizify_csv_ingest'; assert 'dependencies' not in d['project']; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f quizify-csv-to-json-webhook/pyproject.toml`
    - Project name: `grep -c '^name = "quizify-csv-to-json-webhook"' quizify-csv-to-json-webhook/pyproject.toml` returns `1`
    - Version: `grep -c '^version = "1.1.0"' quizify-csv-to-json-webhook/pyproject.toml` returns `1`
    - Python floor: `grep -c '^requires-python = ">=3.9"' quizify-csv-to-json-webhook/pyproject.toml` returns `1`
    - Locked extra (Pitfall 24): `grep -c '^validate = \["fastjsonschema>=2.21.2"\]' quizify-csv-to-json-webhook/pyproject.toml` returns `1`
    - Build backend (D-06-02): `grep -c 'flit_core >=3.2,<4' quizify-csv-to-json-webhook/pyproject.toml` returns `1`
    - Build backend value: `grep -c 'build-backend = "flit_core.buildapi"' quizify-csv-to-json-webhook/pyproject.toml` returns `1`
    - Single-module declaration: `grep -c '^\[tool.flit.module\]' quizify-csv-to-json-webhook/pyproject.toml` returns `1`
    - Module name: `grep -c '^name = "quizify_csv_ingest"' quizify-csv-to-json-webhook/pyproject.toml` returns `1`
    - D-13 + Pitfall 20: NO `[project.dependencies]` table — `grep -c '^\[project.dependencies\]' quizify-csv-to-json-webhook/pyproject.toml` returns `0` AND `grep -c '^dependencies = \[' quizify-csv-to-json-webhook/pyproject.toml` returns `0`
    - Pitfall 24: extra name appears nowhere as `validation`, `Validate`, or `validate-extras`: `grep -ci 'validation\|validate-extras' quizify-csv-to-json-webhook/pyproject.toml` returns `0`
    - fastjsonschema appears EXACTLY once (Pitfall 20 — should never appear in `[project.dependencies]`): `grep -c 'fastjsonschema' quizify-csv-to-json-webhook/pyproject.toml` returns `1`
    - tomllib parse + structural assertions pass (see automated verify above) — exits 0 with `ok`
    - Existing test suite stays green: `cd quizify-csv-to-json-webhook && pytest -q` exits 0
  </acceptance_criteria>
  <done>pyproject.toml exists with the locked PEP 621 metadata, the locked `[validate]` extra, the locked flit_core build backend, and an empty/absent `[project.dependencies]`; tomllib parse-and-assert verifies all six locked keys; existing 71-test suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pyproject.toml ↔ `pip install` | The optional extra is resolved at install time; supply chain risk is the package source on PyPI. |
| author ↔ runtime classification | A typo placing fastjsonschema under `[project.dependencies]` (instead of optional) silently breaks D-13 stdlib-only-at-runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-DEP-01 | Tampering / Spoofing (supply chain) | `[project.optional-dependencies] validate` | mitigate | (a) Pin lower bound at `>=2.21.2` (the exact version researched against). (b) Gate behind `--validate` AND lazy import in Plan 03 — the dep never loads unless the operator opts in. (c) D-13 stdlib-only-at-runtime preserved by the absent `[project.dependencies]` table. (d) Acceptance criterion `grep -c '^\[project.dependencies\]' ... returns 0` enforces (c) in CI. fastjsonschema package on PyPI is published by `horejsek` since 2017 (RESEARCH §"Security Domain") — well-established, unambiguous name. |
| T-PII-01 (carry-forward) | Information Disclosure | packaging | accept | pyproject.toml carries no row data; no PII surface. |
</threat_model>

<verification>
- `cd quizify-csv-to-json-webhook && python -c "import tomllib; tomllib.loads(open('pyproject.toml','rb').read().decode('utf-8'))"` — TOML parses without error.
- `cd quizify-csv-to-json-webhook && pytest -q` — full suite stays green (no test depends on pyproject.toml's contents yet; this confirms zero regression).
- Manual smoke (post-Plan-03): `cd quizify-csv-to-json-webhook && python -m venv /tmp/venv6 && /tmp/venv6/bin/pip install '.[validate]'` — should resolve fastjsonschema 2.21.2.
</verification>

<success_criteria>
- pyproject.toml exists at `quizify-csv-to-json-webhook/pyproject.toml`.
- All six locked PEP 621 keys present and exactly match D-06-01..D-06-05.
- `[project.dependencies]` is absent (D-13 + Pitfall 20).
- `[validate]` extra is the literal lowercase singular `validate` (Pitfall 24).
- `[tool.flit.module] name = "quizify_csv_ingest"` decouples project name from module name (D-06-04).
- Existing 71-test suite stays green.
</success_criteria>

<output>
After completion, create `.planning/phases/06-json-schema-validation/06-02-SUMMARY.md`
</output>
