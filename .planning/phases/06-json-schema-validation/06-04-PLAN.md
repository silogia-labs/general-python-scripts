---
phase: 06-json-schema-validation
plan: 04
type: execute
wave: 2
depends_on: ["06-03"]
files_modified:
  - quizify-csv-to-json-webhook/README.md
  - .planning/PROJECT.md
autonomous: true
requirements: [VALI-06]
tags: [docs, readme, decision-log, d-11]

must_haves:
  truths:
    - "README's `## CLI reference` table contains a `--validate` row (default `off`; description naming `docs/webhook-schema.json` and the `[validate]` extra; env var `—`)."
    - "README's `## Quickstart` section contains an OPTIONAL second install line `pip install '.[validate]'`."
    - "README's existing 10 H2 sections are unchanged in count and order — D-11 drift test (`tests/test_readme_help_alignment.py`) stays green WITHOUT test edits."
    - "README inline-references `docs/webhook-schema.json` near the existing `docs/webhook-quizify-format-example.json` reference."
    - "PROJECT.md Key Decisions table records the v1.1 VALI-01 ship and confirms D-13 stdlib-only-at-runtime preservation."
  artifacts:
    - path: "quizify-csv-to-json-webhook/README.md"
      provides: "Operator docs for `--validate` flag, `[validate]` extra install, schema artifact path."
      contains: "--validate"
    - path: ".planning/PROJECT.md"
      provides: "Decision-log row for v1.1 Phase 6 close + D-13 carry-forward confirmation."
      contains: "VALI-01"
  key_links:
    - from: "README.md ## CLI reference table"
      to: "argparse `--validate` flag in quizify_csv_ingest.py"
      via: "string substring match enforced by tests/test_readme_help_alignment.py"
      pattern: "\\| `--validate` \\|"
    - from: "README.md ## Quickstart"
      to: "pyproject.toml [validate] extra"
      via: "literal install command"
      pattern: "pip install '\\.\\[validate\\]'"
---

<objective>
Land the operator-facing documentation: extend README's `## CLI reference` table with the `--validate` row, add the optional install line under `## Quickstart`, inline-reference the schema artifact, and append a Phase-6 row to PROJECT.md's Key Decisions table. ZERO new H2 sections (D-11 drift test stays green without test edits).

Purpose: Without this plan, operators don't discover `--validate` and the D-11 drift test catches the missing flag-as-substring on every CI run. With it, VALI-06 ships and Phase 6 closes.
Output: 2 file edits (README.md, PROJECT.md). NO test file changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-json-schema-validation/06-CONTEXT.md
@.planning/phases/06-json-schema-validation/06-RESEARCH.md
@.planning/phases/06-json-schema-validation/06-PATTERNS.md
@.planning/phases/06-json-schema-validation/06-01-PLAN.md
@.planning/phases/06-json-schema-validation/06-02-PLAN.md
@.planning/phases/06-json-schema-validation/06-03-PLAN.md
@quizify-csv-to-json-webhook/README.md
@quizify-csv-to-json-webhook/tests/test_readme_help_alignment.py

<interfaces>
<!-- D-11 lock: README has EXACTLY these 10 H2 sections in this order. -->

Locked H2 sections (verified 2026-05-04 via `grep -nE "^## " README.md`):
  ## Purpose
  ## Quickstart
  ## CLI reference
  ## Configuration
  ## Column assumptions
  ## Output shape
  ## Limitations
  ## Privacy notes
  ## Exit codes
  ## Development

D-11 drift test rules (per RESEARCH §VALI-06 line 89 + tests/test_readme_help_alignment.py):
  - Asserts the 10 named H2 sections are present.
  - Asserts every long flag from `--help` appears as substring of README.
  - Adding the `--validate` row to the existing `## CLI reference` table satisfies both.

Existing CLI reference table (README.md lines 31-38) — column shape:
  | Flag | Default | Description | Env var |
  |------|---------|-------------|---------|

New row to APPEND (D-06-23 verbatim from CONTEXT):
  | `--validate` | off | Validate emitted JSON against `docs/webhook-schema.json` (requires `[validate]` extra). | — |

Existing Quickstart (README.md lines 16-24) — install/run command and inline doc reference.

New install line to ADD under `## Quickstart` (after the existing `python quizify_csv_ingest.py ...` block, before the next H2):
  Optional: enable schema validation
    cd quizify-csv-to-json-webhook
    pip install '.[validate]'
</interfaces>
</context>

<tasks>

<task type="execute">
  <name>Task 1: Extend README.md (D-06-23 — CLI table row + Quickstart install line + inline schema reference)</name>
  <files>quizify-csv-to-json-webhook/README.md</files>
  <read_first>
    - quizify-csv-to-json-webhook/README.md (full file)
    - quizify-csv-to-json-webhook/tests/test_readme_help_alignment.py (the D-11 drift test — what it asserts)
    - .planning/phases/06-json-schema-validation/06-CONTEXT.md (D-06-23)
    - .planning/phases/06-json-schema-validation/06-RESEARCH.md (Pitfall 24 — extra name spelling)
    - .planning/phases/06-json-schema-validation/06-PATTERNS.md (section "README.md")
  </read_first>
  <action>
Make THREE surgical edits to `quizify-csv-to-json-webhook/README.md`. ZERO new H2 sections. ZERO renames. Use the Edit tool, NOT Write — one Edit per change so the diff is reviewable.

**Edit 1 — APPEND row to the existing CLI reference table** (after the `--quiz-title` row at line 38, before the next H2 `## Configuration` at line 40). EXACT new row text:

```
| `--validate` | off | Validate emitted JSON against `docs/webhook-schema.json` (requires `[validate]` extra). Validation runs only when JSON output is produced; `--dry-run` skips it. | — |
```

The row appends to the existing pipe-delimited table. The cell text mentions `docs/webhook-schema.json` (inline schema reference satisfies CONTEXT requirement) and the `[validate]` extra (Pitfall 24 — singular lowercase). The trailing dash in the env-var column matches the existing rows' style (em-dash `—`, not hyphen).

**Edit 2 — APPEND optional install block under `## Quickstart`** (after line 24's "target shape" sentence, before the next H2 `## CLI reference` at line 26). EXACT new content:

```

Optional — enable schema validation:

```bash
cd quizify-csv-to-json-webhook
pip install '.[validate]'
```

The `[validate]` extra installs `fastjsonschema` so the `--validate` flag can check the emitted JSON against [`docs/webhook-schema.json`](docs/webhook-schema.json). Default invocations remain stdlib-only.
```

Note: the inner code fence MUST use four backticks at the top of this entire snippet IF the README's existing code fences are three-backtick — actually use three-backtick fences and ensure they don't collide with the surrounding three-backtick fence at lines 18-20. The existing fence at line 20 closes BEFORE the new content begins, so a fresh three-backtick fence is fine.

**Edit 3 — Inline schema reference** in the existing Quickstart "target shape" sentence (line 22-24). Current text reads:

```
Run from `quizify-csv-to-json-webhook/`. Sample input:
[`docs/quizify-submissions.csv`](docs/quizify-submissions.csv); target shape:
[`docs/webhook-quizify-format-example.json`](docs/webhook-quizify-format-example.json).
```

Replace `target shape:` with `target shape:` and append a sentence after the period:

```
Run from `quizify-csv-to-json-webhook/`. Sample input:
[`docs/quizify-submissions.csv`](docs/quizify-submissions.csv); target shape:
[`docs/webhook-quizify-format-example.json`](docs/webhook-quizify-format-example.json);
formal contract: [`docs/webhook-schema.json`](docs/webhook-schema.json).
```

(Single-line edit converting the trailing period to a semicolon and appending the schema reference. Verify the full sentence compiles in any Markdown previewer.)

CONSTRAINTS (do NOT alter):
- ZERO new `## ` H2 sections (D-11 drift test).
- The 10 existing H2 section names and their order remain unchanged.
- The opening prose at lines 1-7 (the "stdlib-only, no third-party dependencies at runtime" claim) stays — `[validate]` is OPT-IN and Pattern from §"Standard Stack" Core (production runtime — opt-in only). If a single qualifier is needed for accuracy, leave it for a v1.2 cleanup; the current sentence is technically still correct because the default-path runtime is unchanged.
- The locked install command form is `pip install '.[validate]'` (singular `validate`, single quotes, dot-bracket form) — Pitfall 24.
- Use `--validate` (with the leading double-hyphen) consistently — `tests/test_readme_help_alignment.py` greps for the long flag substring.

After both edits run the D-11 drift test:
```bash
cd quizify-csv-to-json-webhook && pytest -q tests/test_readme_help_alignment.py
```

It MUST pass without any test-file modification.

Commit message: `docs(06-04): document --validate flag, [validate] extra install, schema path (VALI-06)`
  </action>
  <verify>
    <automated>cd quizify-csv-to-json-webhook && pytest -q tests/test_readme_help_alignment.py && pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `--validate` flag substring present (D-11 drift test prerequisite): `grep -c '`--validate`' quizify-csv-to-json-webhook/README.md` returns at least `1`
    - Locked extra spelling (Pitfall 24): `grep -c "pip install '\\.\\[validate\\]'" quizify-csv-to-json-webhook/README.md` returns at least `1`
    - Schema artifact referenced inline: `grep -c 'docs/webhook-schema.json' quizify-csv-to-json-webhook/README.md` returns at least `2` (one in CLI table cell, one in Quickstart)
    - CLI reference table extended (table row containing `--validate`): `grep -cE '^\| `--validate` \|' quizify-csv-to-json-webhook/README.md` returns at least `1`
    - D-11 H2-count lock — exactly 10 H2 sections, no additions: `grep -cE '^## ' quizify-csv-to-json-webhook/README.md` returns `10`
    - D-11 H2-order lock — sections in locked order: `grep -nE '^## ' quizify-csv-to-json-webhook/README.md | awk -F: '{print $3}' | tr '\\n' '|'` outputs `## Purpose|## Quickstart|## CLI reference|## Configuration|## Column assumptions|## Output shape|## Limitations|## Privacy notes|## Exit codes|## Development|`
    - D-11 drift test green: `cd quizify-csv-to-json-webhook && pytest -q tests/test_readme_help_alignment.py` exits 0
    - Pitfall 24: NO `'.[validation]'` or `'.[Validate]'` mis-spellings: `grep -ciE "\\.\\[validation\\]|\\.\\[validate-extras\\]|\\.\\[Validate\\]" quizify-csv-to-json-webhook/README.md` returns `0`
    - Full suite stays green: `cd quizify-csv-to-json-webhook && pytest -q` exits 0
  </acceptance_criteria>
  <done>README extended with `--validate` row in CLI table, optional install block under Quickstart, and inline schema reference; 10 H2 sections preserved in order; D-11 drift test green without test modifications.</done>
</task>

<task type="execute">
  <name>Task 2: Append v1.1 Phase 6 row to PROJECT.md Key Decisions table</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (full file — find the Key Decisions table)
    - .planning/phases/06-json-schema-validation/06-CONTEXT.md (`### Carry-forward` block — D-13 confirmation)
    - .planning/STATE.md (v1.1 locked decisions)
  </read_first>
  <action>
Append a single row to the existing Key Decisions table in `.planning/PROJECT.md` capturing the Phase-6 ship.

Steps:
1. Read `.planning/PROJECT.md` and locate the Key Decisions table (its exact column shape and existing v1.1 rows).
2. Match the column shape of the LAST existing row (do not invent new columns).
3. Append a row that records:
   - Decision ID: `VALI-01`
   - Reference: `Phase 6 / D-06-01..D-06-25`
   - Summary: `--validate flag (opt-in) backed by Draft-07 schema at docs/webhook-schema.json; fastjsonschema 2.21.2 as optional [validate] extra; lazy import preserves D-13 stdlib-only-at-runtime; PII-safe stderr templates locked (D-06-19, D-06-20).`
   - Date: `2026-05-04`
4. If the table has a "Status" column, mark this row `Shipped` (or whatever value the existing v1.1-shipped rows use — match style; don't invent).
5. Do NOT alter any existing rows. Do NOT add new columns. Do NOT add new sections.

If a "Constraints" or "Carry-forward" sub-section already records D-13 stdlib-only-at-runtime, leave it untouched — the new row references D-13 by mention only, since that decision was locked in v1.0 and we are confirming preservation, not re-deciding.

After the edit, verify:
```bash
grep -c "VALI-01" .planning/PROJECT.md
```
should return at least `2` (the existing reference plus this new row), and the file should still parse as Markdown (no broken table separators).

Commit message: `docs(06-04): record VALI-01 ship in PROJECT.md Key Decisions (Phase 6 close)`
  </action>
  <verify>
    <automated>grep -c "VALI-01" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - VALI-01 mentioned in PROJECT.md at least twice (existing v1.1 entry + new ship row): `grep -c 'VALI-01' .planning/PROJECT.md` returns at least `2`
    - The new row references docs/webhook-schema.json: `grep -c 'docs/webhook-schema.json' .planning/PROJECT.md` returns at least `1`
    - The new row references the [validate] extra: `grep -c '\[validate\] extra\|\.\[validate\]' .planning/PROJECT.md` returns at least `1`
    - D-13 stdlib-only-at-runtime mention preserved/added: `grep -ci 'stdlib-only' .planning/PROJECT.md` returns at least `1`
    - Phase 6 date stamp added: `grep -c '2026-05-04' .planning/PROJECT.md` returns at least `1`
    - File still parses (no broken table — heuristic: equal pipe count on header and at least one data row): `python -c "import re; lines=open('.planning/PROJECT.md').read().splitlines(); 
# sanity: number of '|' on any header row == on its separator
ok=True
for i,l in enumerate(lines[:-1]):
    if l.startswith('|') and lines[i+1].startswith('|') and set(lines[i+1].replace('|','').replace('-','').replace(':','').strip())==set():
        if l.count('|') != lines[i+1].count('|'): ok=False
print('ok' if ok else 'BROKEN')"` prints `ok`
    - No new H1/H2 sections added accidentally: `grep -cE '^# |^## ' .planning/PROJECT.md` returns the same value before and after this edit (capture pre-edit count from the read in `read_first`; assert no delta)
  </acceptance_criteria>
  <done>PROJECT.md Key Decisions table has a new row recording VALI-01 ship with D-06 decision-ID range, schema path, [validate] extra, and D-13 carry-forward confirmation. No structural changes to the document.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README docs ↔ runtime CLI | README documents `--validate`; the D-11 drift test (`test_readme_help_alignment.py`) is the bidirectional contract enforcer. |
| Decision log ↔ ship state | PROJECT.md is the canonical decision record; new entries should not silently mutate prior decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-DOC-DRIFT-01 | Repudiation / Tampering | README ↔ CLI flag set | mitigate | The existing `tests/test_readme_help_alignment.py` runs every CI build; if `--validate` is added to argparse without README mention, the test fails. By documenting `--validate` in the same milestone the flag ships, drift is prevented. Acceptance criterion `grep -c '`--validate`' README.md returns at least 1` provides a static gate. |
| T-PII-01 (carry-forward) | Information Disclosure | README example values | accept | README does not contain example PII (verified by reading lines 1-80). The new `--validate` row description is categorical only. T-PII-02 (no example PII in schema) is enforced separately in Plan 01. |
| T-DEP-01 (carry-forward) | Tampering / Spoofing | install instructions | accept | The README documents the operator-facing install command verbatim; the actual supply-chain mitigation (lower-bound pin + lazy import + opt-in only) is enforced in Plans 02 and 03. README is descriptive, not enforcement. |
| T-PARSE-01 (carry-forward) | Tampering / Availability | (n/a) | accept | Schema parse errors are surfaced by the helper in Plan 03. README has no parsing surface of its own. |
</threat_model>

<verification>
- `cd quizify-csv-to-json-webhook && pytest -q tests/test_readme_help_alignment.py` — D-11 drift test green without test edits (VALI-06 verified).
- `cd quizify-csv-to-json-webhook && pytest -q` — full 79-test suite green; no regressions.
- `grep -cE '^## ' quizify-csv-to-json-webhook/README.md` — returns exactly `10` (D-11 H2-count lock).
- `grep -c '`--validate`' quizify-csv-to-json-webhook/README.md` — returns at least `1`.
- `grep -c 'VALI-01' .planning/PROJECT.md` — returns at least `2` (v1.1-locked entry + new ship row).
</verification>

<success_criteria>
- VALI-06 satisfied: README documents `--validate` flag, `[validate]` extra install path, and the schema file path; D-11 drift test passes without test modifications.
- D-11 lock preserved: 10 H2 sections, in locked order, no additions.
- PROJECT.md Key Decisions table reflects Phase 6 ship with reference to D-06-01..D-06-25 and D-13 carry-forward.
- Pitfall 24 enforced: README's install line is the literal `pip install '.[validate]'` — singular lowercase.
- Full 79-test suite remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/06-json-schema-validation/06-04-SUMMARY.md`
</output>
