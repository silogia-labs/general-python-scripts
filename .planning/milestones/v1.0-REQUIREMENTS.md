# Requirements Archive: v1.0 MVP

> **Status:** ✅ SHIPPED 2026-05-03
> **Source:** snapshot of `.planning/REQUIREMENTS.md` taken at v1.0 milestone close.
> All 11 v1 requirements (CONV-01..06, WEB-01..05, OPS-01) shipped. Two were *Adjusted* during execution (CONV-05 pass-through-with-advisory rather than forced ISO; WEB-03 omits `id` rather than nulling it) — see Outcome column for detail.

---

# Requirements: Quizify CSV → Webhook JSON

**Defined:** 2026-05-03
**Core Value:** CSV submission rows become webhook-compatible JSON objects without manual restructuring.

## v1 Requirements

### CLI & ingestion

- [x] **CONV-01**: The tool accepts an input CSV path (UTF-8) and produces JSON output (single array file or stdout), suitable for piping into webhook receivers or saving for replay.
- [x] **CONV-02**: Fixed columns are distinguished from dynamic quiz question columns using the known export layout (contact fields first, `Result logic` … `Date` trailer block); remaining headers map to `question-N` indices in order.

### Contact & subscription fields

- [x] **CONV-03**: Maps `First name` → `firstName`, `Last name` → `lastName`, `Email` → `email`, `Phone` → `phone`.
- [x] **CONV-04**: Maps newsletter/subscription column values to `status` (e.g. subscribed vs unsubscribed) consistently with the example payload semantics.
- [x] **CONV-05**: Maps submission `Date` to `statusDate` using ISO `YYYY-MM-DD` (or documented timezone assumptions if needed).

### Normalization & answers

- [x] **CONV-06**: Decodes HTML entities in CSV cell text so JSON strings match human-readable quiz answers (e.g. `&gt;` → `>`).
- [x] **WEB-01**: Builds `tags`, including a Quizify source marker aligned with the example, and merges parsed values from the `Answer tags` column where present.
- [x] **WEB-02**: For each dynamic question index `N`, emits `question-N` (verbatim header text), `answers-N`, and `answers-tags-N` when appropriate.
- [x] **WEB-03**: Chooses answer representation per row: plain string for free-text / composite answers; array of objects with `answer_name` (and optional `answer_img`, `answer_tag`) when modeling discrete choices—omit unknown `id` rather than guessing.

### Scoring & metadata

- [x] **WEB-04**: Maps `Result logic`, `Score category`, and `Score value` into the webhook fields used for recommendations (e.g. `product-recommendation`) following conventions illustrated by the example and documented in the script README.
- [x] **WEB-05**: Supports setting `quiz_title` via CLI flag or env when the CSV does not include a dedicated column.

### Documentation

- [x] **OPS-01**: Adds a concise README beside the script describing usage, column assumptions, and limitations (missing IDs, encoding).

## v2 Requirements

### Automation

- **AUTO-01**: Optional HTTP POST mode to send each payload directly to a webhook URL (batch + retries).

### Validation

- **VALI-01**: Optional JSON Schema validation against a checked-in schema derived from the example file.

## Out of Scope

| Feature | Reason |
|---------|--------|
| Interactive Quizify authentication / scraping | Exports are manual CSV downloads; stay offline-first |
| GUI | Utility script only |
| Multi-tenant Quiz configuration UI | Single-quiz mapping driven by headers is sufficient for v1 |

## Traceability

| Requirement | Phase | Status | Outcome |
|-------------|-------|--------|---------|
| CONV-01 | Phase 1 | Complete | Validated — `csv_path` positional + `-o`/`--output`/stdout in `quizify_csv_ingest.py` |
| CONV-02 | Phase 1 | Complete | Validated — `classify_headers` (6 contact + dynamic + 6 trailer) |
| CONV-03 | Phase 2 | Complete | Validated — verbatim contact mapping in `build_row` (no email/phone case-normalization, per D-10) |
| CONV-04 | Phase 2 | Complete | Validated — `map_status` Yes/No/empty/other branches with PII-safe warning on unexpected value |
| CONV-05 | Phase 2 | Complete | Adjusted — pass-through verbatim with non-ISO advisory warning (D-12) instead of forced ISO conversion |
| CONV-06 | Phase 2 | Complete | Validated — `html.unescape` applied uniformly to cells and headers |
| WEB-01 | Phase 2 | Complete | Validated — `tags` seeded with `source: quizify`; per-row tags merged via `match_tags_to_questions` |
| WEB-02 | Phase 2 | Complete | Validated — every dynamic N emits the `question-N`/`answers-N`/`answers-tags-N` trio (stable across rows) |
| WEB-03 | Phase 2 | Complete | Adjusted — answer shape heuristic (string vs single-element object array); `id` key omitted entirely (D-07) rather than nulled |
| WEB-04 | Phase 3 | Complete | Validated — `result-logic`/`score-category`/`score-value` pass-through + 4 reserved placeholders with locked D-02 defaults |
| WEB-05 | Phase 3 | Complete | Validated — `--quiz-title` CLI flag, `$QUIZIFY_QUIZ_TITLE` env fallback, `""` default; precedence locked by `_resolve_quiz_title` |
| OPS-01 | Phase 3 | Complete | Validated — 169-line operator README per D-11 ten-section structure + automated `--help` drift smoke test |

**Coverage:**

- v1 requirements: 11 total
- Mapped to phases: 11
- Shipped: 11 ✓
- Unmapped: 0 ✓

---
*Requirements defined: 2026-05-03*
*Last updated: 2026-05-03 at v1.0 milestone close*
